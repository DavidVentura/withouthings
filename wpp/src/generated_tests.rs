// @generated by tools/emit_rust.py from tools/wpp.json.
// Source of truth is the Withings APK; edit the emitter, not this file.
#![allow(clippy::all)]

use crate::codec::WppObjectCodec;
use crate::objects::*;

#[test]
fn default_data_size_matches_apk() {
    assert_eq!(Null::default().data_size(), 0, "Null");
    assert_eq!(BatteryPercent::default().data_size(), 2, "BatteryPercent");
    assert_eq!(Bssid::default().data_size(), 7, "Bssid");
    assert_eq!(Wpakey::default().data_size(), 33, "Wpakey");
    assert_eq!(Wifidelay::default().data_size(), 6, "Wifidelay");
    assert_eq!(Ifsettings::default().data_size(), 6, "Ifsettings");
    assert_eq!(WifiEnable::default().data_size(), 1, "WifiEnable");
    assert_eq!(Ifstate::default().data_size(), 5, "Ifstate");
    assert_eq!(Cmderror::default().data_size(), 6, "Cmderror");
    assert_eq!(
        StoredMeasureAction::default().data_size(),
        2,
        "StoredMeasureAction"
    );
    assert_eq!(
        StoredMeasureStatus::default().data_size(),
        7,
        "StoredMeasureStatus"
    );
    assert_eq!(
        StoredMeasureMeta::default().data_size(),
        23,
        "StoredMeasureMeta"
    );
    assert_eq!(
        StoredMeasureData::default().data_size(),
        8,
        "StoredMeasureData"
    );
    assert_eq!(ConnectReason::default().data_size(), 2, "ConnectReason");
    assert_eq!(UserUnit::default().data_size(), 6, "UserUnit");
    assert_eq!(DebugDumpMask::default().data_size(), 4, "DebugDumpMask");
    assert_eq!(DebugDumpType::default().data_size(), 8, "DebugDumpType");
    assert_eq!(CommSupport::default().data_size(), 1, "CommSupport");
    assert_eq!(
        BatteryVoltageMv::default().data_size(),
        2,
        "BatteryVoltageMv"
    );
    assert_eq!(
        DebugDumpIgnoreData::default().data_size(),
        4,
        "DebugDumpIgnoreData"
    );
    assert_eq!(AlarmId::default().data_size(), 1, "AlarmId");
    assert_eq!(NbAlarm::default().data_size(), 1, "NbAlarm");
    assert_eq!(PlsReturnCode::default().data_size(), 16, "PlsReturnCode");
    assert_eq!(PlsStation::default().data_size(), 6, "PlsStation");
    assert_eq!(AppProbe::default().data_size(), 6, "AppProbe");
    assert_eq!(
        StoredMeasureMetaExtend::default().data_size(),
        1,
        "StoredMeasureMetaExtend"
    );
    assert_eq!(FactoryState::default().data_size(), 1, "FactoryState");
    assert_eq!(DebugDumpFormat::default().data_size(), 1, "DebugDumpFormat");
    assert_eq!(
        FactoryResetMode::default().data_size(),
        4,
        "FactoryResetMode"
    );
    assert_eq!(TrackerWearPos::default().data_size(), 1, "TrackerWearPos");
    assert_eq!(CaptureSct01::default().data_size(), 69, "CaptureSct01");
    assert_eq!(FeatureMask::default().data_size(), 4, "FeatureMask");
    assert_eq!(
        ScaleMedappUserInfo::default().data_size(),
        10,
        "ScaleMedappUserInfo"
    );
    assert_eq!(McuId::default().data_size(), 8, "McuId");
    assert_eq!(
        SoftdeviceVersion::default().data_size(),
        12,
        "SoftdeviceVersion"
    );
    assert_eq!(HrAutoAlgo::default().data_size(), 1, "HrAutoAlgo");
    assert_eq!(RhTemp::default().data_size(), 8, "RhTemp");
    assert_eq!(
        WorkoutScreenList::default().data_size(),
        33,
        "WorkoutScreenList"
    );
    assert_eq!(SyncRequest::default().data_size(), 4, "SyncRequest");
    assert_eq!(
        WorkoutGpsStatus::default().data_size(),
        2,
        "WorkoutGpsStatus"
    );
    assert_eq!(Silent::default().data_size(), 1, "Silent");
    assert_eq!(
        StoredSignalMeta::default().data_size(),
        8,
        "StoredSignalMeta"
    );
    assert_eq!(Id::default().data_size(), 4, "Id");
    assert_eq!(
        StoredSignalMetaExtend::default().data_size(),
        12,
        "StoredSignalMetaExtend"
    );
    assert_eq!(
        UnitConversionParameters::default().data_size(),
        12,
        "UnitConversionParameters"
    );
    assert_eq!(
        AlgorithmVersion::default().data_size(),
        8,
        "AlgorithmVersion"
    );
    assert_eq!(FirmwareVersion::default().data_size(), 4, "FirmwareVersion");
    assert_eq!(RawDataCmd::default().data_size(), 1, "RawDataCmd");
    assert_eq!(SignalAttribut::default().data_size(), 4, "SignalAttribut");
    assert_eq!(WifiAnt::default().data_size(), 3, "WifiAnt");
    assert_eq!(Backlight::default().data_size(), 3, "Backlight");
    assert_eq!(Lcd::default().data_size(), 3, "Lcd");
    assert_eq!(Gpio::default().data_size(), 6, "Gpio");
    assert_eq!(Uint32::default().data_size(), 4, "Uint32");
    assert_eq!(Audiotest::default().data_size(), 5, "Audiotest");
    assert_eq!(Weighttest::default().data_size(), 26, "Weighttest");
    assert_eq!(Adc::default().data_size(), 12, "Adc");
    assert_eq!(Dac::default().data_size(), 8, "Dac");
    assert_eq!(WeightCal::default().data_size(), 72, "WeightCal");
    assert_eq!(Zmeter::default().data_size(), 14, "Zmeter");
    assert_eq!(ZmeterCal::default().data_size(), 44, "ZmeterCal");
    assert_eq!(Spiflash::default().data_size(), 2, "Spiflash");
    assert_eq!(Rtc::default().data_size(), 6, "Rtc");
    assert_eq!(WeightVerif::default().data_size(), 29, "WeightVerif");
    assert_eq!(Dump::default().data_size(), 9, "Dump");
    assert_eq!(Selftest::default().data_size(), 3, "Selftest");
    assert_eq!(Slot::default().data_size(), 7, "Slot");
    assert_eq!(Int32::default().data_size(), 4, "Int32");
    assert_eq!(Uint64::default().data_size(), 8, "Uint64");
    assert_eq!(UpdateFirmware::default().data_size(), 5, "UpdateFirmware");
    assert_eq!(
        NetupdateProgress::default().data_size(),
        1,
        "NetupdateProgress"
    );
    assert_eq!(NetupdateResult::default().data_size(), 1, "NetupdateResult");
    assert_eq!(TimeSet::default().data_size(), 16, "TimeSet");
    assert_eq!(TimeSetReply::default().data_size(), 4, "TimeSetReply");
    assert_eq!(BatteryStatus::default().data_size(), 10, "BatteryStatus");
    assert_eq!(
        WamDisplayedInfo::default().data_size(),
        20,
        "WamDisplayedInfo"
    );
    assert_eq!(WamVasistasGet::default().data_size(), 6, "WamVasistasGet");
    assert_eq!(
        WamDailyActivities::default().data_size(),
        20,
        "WamDailyActivities"
    );
    assert_eq!(WamScreensList::default().data_size(), 25, "WamScreensList");
    assert_eq!(WamAutoSleep::default().data_size(), 4, "WamAutoSleep");
    assert_eq!(AlarmSetSimple::default().data_size(), 2, "AlarmSetSimple");
    assert_eq!(HandsMove::default().data_size(), 3, "HandsMove");
    assert_eq!(DemoStart::default().data_size(), 5, "DemoStart");
    assert_eq!(SetTime::default().data_size(), 8, "SetTime");
    assert_eq!(TrackerGoal::default().data_size(), 8, "TrackerGoal");
    assert_eq!(Alarm::default().data_size(), 7, "Alarm");
    assert_eq!(VasistasType::default().data_size(), 4, "VasistasType");
    assert_eq!(ScreenList::default().data_size(), 18, "ScreenList");
    assert_eq!(WamVasistasHead::default().data_size(), 4, "WamVasistasHead");
    assert_eq!(
        WamVasistasDuration::default().data_size(),
        2,
        "WamVasistasDuration"
    );
    assert_eq!(
        WamVasistasAwake::default().data_size(),
        14,
        "WamVasistasAwake"
    );
    assert_eq!(WamVasistasWalk::default().data_size(), 2, "WamVasistasWalk");
    assert_eq!(WamVasistasRun::default().data_size(), 2, "WamVasistasRun");
    assert_eq!(
        WamVasistasSleepDbg::default().data_size(),
        2,
        "WamVasistasSleepDbg"
    );
    assert_eq!(
        WamVasistasSleep::default().data_size(),
        2,
        "WamVasistasSleep"
    );
    assert_eq!(
        WamVasistasMetCal::default().data_size(),
        4,
        "WamVasistasMetCal"
    );
    assert_eq!(VasistasSwimV1::default().data_size(), 5, "VasistasSwimV1");
    assert_eq!(
        WamVasistasMetCalEarned::default().data_size(),
        4,
        "WamVasistasMetCalEarned"
    );
    assert_eq!(
        VasistasActiRecoV1V2::default().data_size(),
        4,
        "VasistasActiRecoV1V2"
    );
    assert_eq!(
        VasistasUncertainSwim::default().data_size(),
        1,
        "VasistasUncertainSwim"
    );
    assert_eq!(
        VasistasSwimType::default().data_size(),
        1,
        "VasistasSwimType"
    );
    assert_eq!(WpmSetMode::default().data_size(), 1, "WpmSetMode");
    assert_eq!(WpmStart::default().data_size(), 1, "WpmStart");
    assert_eq!(WpmResult::default().data_size(), 20, "WpmResult");
    assert_eq!(WpmBpPulse::default().data_size(), 16, "WpmBpPulse");
    assert_eq!(WpmEvent::default().data_size(), 1, "WpmEvent");
    assert_eq!(WpmSetValve::default().data_size(), 2, "WpmSetValve");
    assert_eq!(WpmSetMotor::default().data_size(), 2, "WpmSetMotor");
    assert_eq!(WpmGetPressure::default().data_size(), 2, "WpmGetPressure");
    assert_eq!(WpmGetzero::default().data_size(), 1, "WpmGetzero");
    assert_eq!(WpmPressure::default().data_size(), 6, "WpmPressure");
    assert_eq!(WsmMode::default().data_size(), 1, "WsmMode");
    assert_eq!(WsmValve::default().data_size(), 2, "WsmValve");
    assert_eq!(WsmMotor::default().data_size(), 2, "WsmMotor");
    assert_eq!(
        WsmPressureMvtGet::default().data_size(),
        2,
        "WsmPressureMvtGet"
    );
    assert_eq!(WsmZeroGet::default().data_size(), 1, "WsmZeroGet");
    assert_eq!(WsmVasistasGet::default().data_size(), 6, "WsmVasistasGet");
    assert_eq!(WsmLedControl::default().data_size(), 19, "WsmLedControl");
    assert_eq!(WsmPrivateMode::default().data_size(), 1, "WsmPrivateMode");
    assert_eq!(
        WsmVasistasGeneral::default().data_size(),
        37,
        "WsmVasistasGeneral"
    );
    assert_eq!(
        WsmVasistasGeneralV2::default().data_size(),
        62,
        "WsmVasistasGeneralV2"
    );
    assert_eq!(
        WsdScanWsmFinish::default().data_size(),
        1,
        "WsdScanWsmFinish"
    );
    assert_eq!(WsdAlarm::default().data_size(), 8, "WsdAlarm");
    assert_eq!(
        WsdSetWsmUserResult::default().data_size(),
        1,
        "WsdSetWsmUserResult"
    );
    assert_eq!(
        WsdProgramSettings::default().data_size(),
        4,
        "WsdProgramSettings"
    );
    assert_eq!(WsdStatus::default().data_size(), 1, "WsdStatus");
    assert_eq!(WsdProgramId::default().data_size(), 2, "WsdProgramId");
    assert_eq!(WsdProgramInfo::default().data_size(), 5, "WsdProgramInfo");
    assert_eq!(SetClockMode::default().data_size(), 1, "SetClockMode");
    assert_eq!(Color::default().data_size(), 8, "Color");
    assert_eq!(LampState::default().data_size(), 1, "LampState");
    assert_eq!(
        ClockDisplaySetting::default().data_size(),
        2,
        "ClockDisplaySetting"
    );
    assert_eq!(BleLinkStatus::default().data_size(), 2, "BleLinkStatus");
    assert_eq!(WsdSubstatus::default().data_size(), 1, "WsdSubstatus");
    assert_eq!(WppEvtSupport::default().data_size(), 1, "WppEvtSupport");
    assert_eq!(SwimPoolSize::default().data_size(), 1, "SwimPoolSize");
    assert_eq!(ColorHsl::default().data_size(), 4, "ColorHsl");
    assert_eq!(ColorHsv::default().data_size(), 4, "ColorHsv");
    assert_eq!(ColorRgb::default().data_size(), 6, "ColorRgb");
    assert_eq!(
        ResponsiveLightMode::default().data_size(),
        1,
        "ResponsiveLightMode"
    );
    assert_eq!(ProgramType::default().data_size(), 5, "ProgramType");
    assert_eq!(ReturnCode::default().data_size(), 4, "ReturnCode");
    assert_eq!(AlarmEnabled::default().data_size(), 1, "AlarmEnabled");
    assert_eq!(TappingStatus::default().data_size(), 1, "TappingStatus");
    assert_eq!(SwimStatus::default().data_size(), 1, "SwimStatus");
    assert_eq!(
        CalibrationSession::default().data_size(),
        8,
        "CalibrationSession"
    );
    assert_eq!(WsdSubprogram::default().data_size(), 4, "WsdSubprogram");
    assert_eq!(SendEnvMeas::default().data_size(), 4, "SendEnvMeas");
    assert_eq!(DispPrefs0::default().data_size(), 1, "DispPrefs0");
    assert_eq!(DispBehavior0::default().data_size(), 1, "DispBehavior0");
    assert_eq!(HrAs7000Stop::default().data_size(), 1, "HrAs7000Stop");
    assert_eq!(HrAs7000::default().data_size(), 17, "HrAs7000");
    assert_eq!(
        AppProbeOsVersion::default().data_size(),
        2,
        "AppProbeOsVersion"
    );
    assert_eq!(
        VasistasHeartrate::default().data_size(),
        4,
        "VasistasHeartrate"
    );
    assert_eq!(AncsStatus::default().data_size(), 1, "AncsStatus");
    assert_eq!(AncsConfig::default().data_size(), 2, "AncsConfig");
    assert_eq!(
        VasistasActivityStatus::default().data_size(),
        1,
        "VasistasActivityStatus"
    );
    assert_eq!(TempCalSet::default().data_size(), 5, "TempCalSet");
    assert_eq!(TempCalReply::default().data_size(), 9, "TempCalReply");
    assert_eq!(CalibrationType::default().data_size(), 1, "CalibrationType");
    assert_eq!(
        CalibrationPoint::default().data_size(),
        9,
        "CalibrationPoint"
    );
    assert_eq!(VasistasWristHr::default().data_size(), 2, "VasistasWristHr");
    assert_eq!(
        VasistasWristTemp::default().data_size(),
        2,
        "VasistasWristTemp"
    );
    assert_eq!(HomeScreen::default().data_size(), 1, "HomeScreen");
    assert_eq!(
        LightSensorReply::default().data_size(),
        8,
        "LightSensorReply"
    );
    assert_eq!(
        CustomizationIdGet::default().data_size(),
        4,
        "CustomizationIdGet"
    );
    assert_eq!(
        CustomizationIdSet::default().data_size(),
        4,
        "CustomizationIdSet"
    );
    assert_eq!(LuminosityLevel::default().data_size(), 2, "LuminosityLevel");
    assert_eq!(AnsStatus::default().data_size(), 1, "AnsStatus");
    assert_eq!(AnsConfig::default().data_size(), 2, "AnsConfig");
    assert_eq!(UserAction::default().data_size(), 1, "UserAction");
    assert_eq!(LiveHr::default().data_size(), 1, "LiveHr");
    assert_eq!(UnknownSteps::default().data_size(), 4, "UnknownSteps");
    assert_eq!(TimeCounters::default().data_size(), 24, "TimeCounters");
    assert_eq!(SpiFlashCmd::default().data_size(), 16, "SpiFlashCmd");
    assert_eq!(SpiFlashChunk::default().data_size(), 17, "SpiFlashChunk");
    assert_eq!(VibratorPattern::default().data_size(), 2, "VibratorPattern");
    assert_eq!(HrAs7000Status::default().data_size(), 1, "HrAs7000Status");
    assert_eq!(
        SleepActivityGet::default().data_size(),
        4,
        "SleepActivityGet"
    );
    assert_eq!(SleepActivity::default().data_size(), 36, "SleepActivity");
    assert_eq!(
        EventV1Deprecated::default().data_size(),
        13,
        "EventV1Deprecated"
    );
    assert_eq!(SignalType::default().data_size(), 1, "SignalType");
    assert_eq!(SignalHeader::default().data_size(), 10, "SignalHeader");
    assert_eq!(No2Cal::default().data_size(), 13, "No2Cal");
    assert_eq!(No2CalMeasure::default().data_size(), 16, "No2CalMeasure");
    assert_eq!(LapNb::default().data_size(), 12, "LapNb");
    assert_eq!(BatteryStateOpt::default().data_size(), 4, "BatteryStateOpt");
    assert_eq!(ScreenState::default().data_size(), 1, "ScreenState");
    assert_eq!(Steps::default().data_size(), 4, "Steps");
    assert_eq!(Calories::default().data_size(), 4, "Calories");
    assert_eq!(Distance::default().data_size(), 4, "Distance");
    assert_eq!(AcqFreq::default().data_size(), 4, "AcqFreq");
    assert_eq!(InfoType::default().data_size(), 1, "InfoType");
    assert_eq!(Duration::default().data_size(), 4, "Duration");
    assert_eq!(GlyphId::default().data_size(), 4, "GlyphId");
    assert_eq!(ImageMetadata::default().data_size(), 3, "ImageMetadata");
    assert_eq!(
        RawDataStreamControl::default().data_size(),
        4,
        "RawDataStreamControl"
    );
    assert_eq!(Version::default().data_size(), 1, "Version");
    assert_eq!(RawDataReadMode::default().data_size(), 4, "RawDataReadMode");
    assert_eq!(
        NotificationAppHashIdVersion::default().data_size(),
        1,
        "NotificationAppHashIdVersion"
    );
    assert_eq!(
        NotificationAppHashIdCrc32::default().data_size(),
        4,
        "NotificationAppHashIdCrc32"
    );
    assert_eq!(CacheType::default().data_size(), 1, "CacheType");
    assert_eq!(
        ActivitySubcategory::default().data_size(),
        2,
        "ActivitySubcategory"
    );
    assert_eq!(Timestamp::default().data_size(), 4, "Timestamp");
    assert_eq!(Pace::default().data_size(), 4, "Pace");
    assert_eq!(FaceMode::default().data_size(), 1, "FaceMode");
    assert_eq!(Speed::default().data_size(), 4, "Speed");
    assert_eq!(FwInfo::default().data_size(), 33, "FwInfo");
    assert_eq!(FwChunkRequest::default().data_size(), 14, "FwChunkRequest");
    assert_eq!(FwChunkCrc::default().data_size(), 12, "FwChunkCrc");
    assert_eq!(StartTime::default().data_size(), 4, "StartTime");
    assert_eq!(EndTime::default().data_size(), 4, "EndTime");
    assert_eq!(Status::default().data_size(), 1, "Status");
    assert_eq!(TestScreen::default().data_size(), 1, "TestScreen");
    assert_eq!(
        ChallengeRequest::default().data_size(),
        1,
        "ChallengeRequest"
    );
    assert_eq!(DebugDumpAnchor::default().data_size(), 4, "DebugDumpAnchor");
    assert_eq!(FactoryMode::default().data_size(), 4, "FactoryMode");
    assert_eq!(GlanceStatus::default().data_size(), 1, "GlanceStatus");
    assert_eq!(MeasureCategory::default().data_size(), 2, "MeasureCategory");
    assert_eq!(MeasureLiveMeta::default().data_size(), 4, "MeasureLiveMeta");
    assert_eq!(
        MeasureStopReason::default().data_size(),
        4,
        "MeasureStopReason"
    );
    assert_eq!(
        PressureTemperature::default().data_size(),
        41,
        "PressureTemperature"
    );
    assert_eq!(
        AltimeterCompensation::default().data_size(),
        4,
        "AltimeterCompensation"
    );
    assert_eq!(Percentile::default().data_size(), 1, "Percentile");
    assert_eq!(
        BatteryStatusSamples::default().data_size(),
        67,
        "BatteryStatusSamples"
    );
    assert_eq!(
        MeasureProcessStep::default().data_size(),
        8,
        "MeasureProcessStep"
    );
    assert_eq!(ActivityLap::default().data_size(), 1, "ActivityLap");
    assert_eq!(ActivityPause::default().data_size(), 1, "ActivityPause");
    assert_eq!(PauseState::default().data_size(), 9, "PauseState");
    assert_eq!(WorkoutLapState::default().data_size(), 9, "WorkoutLapState");
    assert_eq!(
        WorkoutTotalDiff::default().data_size(),
        4,
        "WorkoutTotalDiff"
    );
    assert_eq!(RebootReason::default().data_size(), 4, "RebootReason");
    assert_eq!(RebootOptional::default().data_size(), 1, "RebootOptional");
    assert_eq!(
        Max8614xFactoryStats::default().data_size(),
        61,
        "Max8614xFactoryStats"
    );
    assert_eq!(
        DigitalCrownMotionDelta::default().data_size(),
        4,
        "DigitalCrownMotionDelta"
    );
    assert_eq!(
        DigitalCrownResolutionParams::default().data_size(),
        2,
        "DigitalCrownResolutionParams"
    );
    assert_eq!(
        Max8614xFactoryStatsParams::default().data_size(),
        4,
        "Max8614xFactoryStatsParams"
    );
    assert_eq!(MtuWpp::default().data_size(), 2, "MtuWpp");
    assert_eq!(MtuTls::default().data_size(), 2, "MtuTls");
    assert_eq!(MtuAttBle::default().data_size(), 2, "MtuAttBle");
    assert_eq!(
        StoredMeasureDataExtendPos::default().data_size(),
        1,
        "StoredMeasureDataExtendPos"
    );
    assert_eq!(Heartrate::default().data_size(), 1, "Heartrate");
    assert_eq!(VasistasSpo2::default().data_size(), 6, "VasistasSpo2");
    assert_eq!(CovidEbidEcc::default().data_size(), 13, "CovidEbidEcc");
    assert_eq!(
        CovidStatusAtRisk::default().data_size(),
        5,
        "CovidStatusAtRisk"
    );
    assert_eq!(
        FeatureTagsDeprecated::default().data_size(),
        10,
        "FeatureTagsDeprecated"
    );
    assert_eq!(VasistasFlags::default().data_size(), 4, "VasistasFlags");
    assert_eq!(
        MeasureLiveAppStatus::default().data_size(),
        1,
        "MeasureLiveAppStatus"
    );
    assert_eq!(VasistasAhi::default().data_size(), 4, "VasistasAhi");
    assert_eq!(ShortcutAction::default().data_size(), 1, "ShortcutAction");
    assert_eq!(Threshold::default().data_size(), 8, "Threshold");
    assert_eq!(Shortid::default().data_size(), 2, "Shortid");
    assert_eq!(
        NotificationsDisplayState::default().data_size(),
        1,
        "NotificationsDisplayState"
    );
    assert_eq!(
        LocalNotification::default().data_size(),
        5,
        "LocalNotification"
    );
    assert_eq!(EventV2::default().data_size(), 12, "EventV2");
    assert_eq!(Boolean::default().data_size(), 1, "Boolean");
    assert_eq!(EcgWaveItvl::default().data_size(), 8, "EcgWaveItvl");
    assert_eq!(BreatheParam::default().data_size(), 5, "BreatheParam");
    assert_eq!(InactivityCfg::default().data_size(), 5, "InactivityCfg");
    assert_eq!(
        ConsumableStatus::default().data_size(),
        4,
        "ConsumableStatus"
    );
    assert_eq!(StripCount::default().data_size(), 4, "StripCount");
    assert_eq!(
        Max8614xChannelStats::default().data_size(),
        17,
        "Max8614xChannelStats"
    );
    assert_eq!(
        Max8614xChannelConfig::default().data_size(),
        4,
        "Max8614xChannelConfig"
    );
    assert_eq!(Max8614xR::default().data_size(), 4, "Max8614xR");
    assert_eq!(MacBytes::default().data_size(), 7, "MacBytes");
    assert_eq!(
        IsCartridgeInserted::default().data_size(),
        1,
        "IsCartridgeInserted"
    );
    assert_eq!(IsHoodOpened::default().data_size(), 1, "IsHoodOpened");
    assert_eq!(
        Mcp3422MeasureResult::default().data_size(),
        8,
        "Mcp3422MeasureResult"
    );
    assert_eq!(
        As6221MeasureResult::default().data_size(),
        6,
        "As6221MeasureResult"
    );
    assert_eq!(AlgoParam::default().data_size(), 6, "AlgoParam");
    assert_eq!(Stairs::default().data_size(), 4, "Stairs");
    assert_eq!(MoveHands::default().data_size(), 1, "MoveHands");
    assert_eq!(
        CartridgeExpiryDate::default().data_size(),
        16,
        "CartridgeExpiryDate"
    );
    assert_eq!(
        WorkoutMaxNumber::default().data_size(),
        1,
        "WorkoutMaxNumber"
    );
    assert_eq!(
        Tmp117MeasureResult::default().data_size(),
        6,
        "Tmp117MeasureResult"
    );
    assert_eq!(
        Sn19020x6MeasureResult::default().data_size(),
        8,
        "Sn19020x6MeasureResult"
    );
    assert_eq!(
        SkinTemperatureMeasureResult::default().data_size(),
        6,
        "SkinTemperatureMeasureResult"
    );
    assert_eq!(
        FluxSensorMeasureResult::default().data_size(),
        8,
        "FluxSensorMeasureResult"
    );
    assert_eq!(FactoryTestDsc::default().data_size(), 6, "FactoryTestDsc");
    assert_eq!(VasistasCbt::default().data_size(), 6, "VasistasCbt");
    assert_eq!(
        CleansingModeStart::default().data_size(),
        1,
        "CleansingModeStart"
    );
    assert_eq!(
        CleansingModeStatus::default().data_size(),
        9,
        "CleansingModeStatus"
    );
    assert_eq!(
        DigitalCrownScaleFactor::default().data_size(),
        4,
        "DigitalCrownScaleFactor"
    );
    assert_eq!(SymptomType::default().data_size(), 2, "SymptomType");
    assert_eq!(
        CycleTrackingCycle::default().data_size(),
        28,
        "CycleTrackingCycle"
    );
    assert_eq!(
        VibratorSetSimple::default().data_size(),
        5,
        "VibratorSetSimple"
    );
    assert_eq!(
        CycleTrackingMenstruationStart::default().data_size(),
        4,
        "CycleTrackingMenstruationStart"
    );
    assert_eq!(DeviceStatus::default().data_size(), 4, "DeviceStatus");
    assert_eq!(TrackerHistory::default().data_size(), 29, "TrackerHistory");
    assert_eq!(FeatureTags::default().data_size(), 12, "FeatureTags");
    assert_eq!(
        GreentegIntegrationFactor::default().data_size(),
        4,
        "GreentegIntegrationFactor"
    );
    assert_eq!(BleIrk::default().data_size(), 17, "BleIrk");
    assert_eq!(VasistasHrv::default().data_size(), 6, "VasistasHrv");
    assert_eq!(VasistasRr::default().data_size(), 2, "VasistasRr");
    assert_eq!(
        VasistasActivityType::default().data_size(),
        4,
        "VasistasActivityType"
    );
    assert_eq!(VasistasDbt::default().data_size(), 9, "VasistasDbt");
    assert_eq!(
        GreentegParameter::default().data_size(),
        6,
        "GreentegParameter"
    );
    assert_eq!(
        AccelerometerValues::default().data_size(),
        6,
        "AccelerometerValues"
    );
    assert_eq!(
        PushButtonTestStatus::default().data_size(),
        1,
        "PushButtonTestStatus"
    );
    assert_eq!(StatusChanged::default().data_size(), 4, "StatusChanged");
    assert_eq!(SettingsChanged::default().data_size(), 4, "SettingsChanged");
}

/// Encode a populated instance of every type and decode it back, so the
/// length-prefixed and zero-padded paths are exercised rather than the
/// all-empty default.
#[test]
fn every_type_id_round_trips_through_the_enum() {
    {
        let o = WppObject::Null(Null {
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Null size");
        assert_eq!(WppObject::parse(256, &bytes), o, "Null round trip");
    }
    {
        let o = WppObject::ProbeReply(ProbeReply {
            vid: 1,
            pid: 2,
            name: String::from("wpp3"),
            mac: String::from("wpp4"),
            secret: String::from("wpp5"),
            hard_version: 6,
            mfg_id: String::from("wpp7"),
            bl_version: 8,
            soft_version: 9,
            rescue_version: 10,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ProbeReply size");
        assert_eq!(WppObject::parse(257, &bytes), o, "ProbeReply round trip");
    }
    {
        let o = WppObject::WifiScanParam(WifiScanParam {
            ssid: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WifiScanParam size");
        assert_eq!(WppObject::parse(258, &bytes), o, "WifiScanParam round trip");
    }
    {
        let o = WppObject::WifiApScan(WifiApScan {
            ssid: String::from("wpp1"),
            rssi: 2,
            auth_flags: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WifiApScan size");
        assert_eq!(WppObject::parse(259, &bytes), o, "WifiApScan round trip");
    }
    {
        let o = WppObject::WifiApConnect(WifiApConnect {
            ssid: String::from("wpp1"),
            password: String::from("wpp2"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WifiApConnect size");
        assert_eq!(WppObject::parse(260, &bytes), o, "WifiApConnect round trip");
    }
    {
        let o = WppObject::IpSettings(IpSettings {
            ip: vec![1, 2],
            mask: 2,
            gateway_ip: vec![3, 4],
            dns_ip: vec![4, 5],
            proxy_ip: vec![5, 6],
            proxy_port: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "IpSettings size");
        assert_eq!(WppObject::parse(261, &bytes), o, "IpSettings round trip");
    }
    {
        let o = WppObject::WifiConnectResult(WifiConnectResult {
            wifi: 1,
            dhcp: 2,
            dns_resolv: 3,
            web_srv_ip: String::from("wpp4"),
            http: 5,
            ws_assoc: 6,
            blindcode: 7,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WifiConnectResult size");
        assert_eq!(
            WppObject::parse(262, &bytes),
            o,
            "WifiConnectResult round trip"
        );
    }
    {
        let o = WppObject::BatteryPercent(BatteryPercent {
            percent: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BatteryPercent size");
        assert_eq!(
            WppObject::parse(263, &bytes),
            o,
            "BatteryPercent round trip"
        );
    }
    {
        let o = WppObject::WifiCountry(WifiCountry {
            cmd: 1,
            rc: 2,
            country: String::from("wpp3"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WifiCountry size");
        assert_eq!(WppObject::parse(264, &bytes), o, "WifiCountry round trip");
    }
    {
        let o = WppObject::Ip(Ip {
            octet: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Ip size");
        assert_eq!(WppObject::parse(265, &bytes), o, "Ip round trip");
    }
    {
        let o = WppObject::Bssid(Bssid {
            octet: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Bssid size");
        assert_eq!(WppObject::parse(266, &bytes), o, "Bssid round trip");
    }
    {
        let o = WppObject::Wpakey(Wpakey {
            octet: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Wpakey size");
        assert_eq!(WppObject::parse(267, &bytes), o, "Wpakey round trip");
    }
    {
        let o = WppObject::Wifidelay(Wifidelay {
            cmd: 1,
            rc: 2,
            delay: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Wifidelay size");
        assert_eq!(WppObject::parse(268, &bytes), o, "Wifidelay round trip");
    }
    {
        let o = WppObject::Ifsettings(Ifsettings {
            cmd: 1,
            rc: 2,
            flags: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Ifsettings size");
        assert_eq!(WppObject::parse(269, &bytes), o, "Ifsettings round trip");
    }
    {
        let o = WppObject::WifiEnable(WifiEnable {
            enable: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WifiEnable size");
        assert_eq!(WppObject::parse(270, &bytes), o, "WifiEnable round trip");
    }
    {
        let o = WppObject::Ifstate(Ifstate {
            cmd: 1,
            rc: 2,
            iface: 3,
            state: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Ifstate size");
        assert_eq!(WppObject::parse(271, &bytes), o, "Ifstate round trip");
    }
    {
        let o = WppObject::Cmderror(Cmderror {
            cmd: 1,
            err: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Cmderror size");
        assert_eq!(WppObject::parse(272, &bytes), o, "Cmderror round trip");
    }
    {
        let o = WppObject::ConnectResultExt(ConnectResultExt {
            key: 1,
            val_int: 2,
            val_str: String::from("wpp3"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ConnectResultExt size");
        assert_eq!(
            WppObject::parse(273, &bytes),
            o,
            "ConnectResultExt round trip"
        );
    }
    {
        let o = WppObject::ScaleSession(ScaleSession {
            data: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ScaleSession size");
        assert_eq!(WppObject::parse(274, &bytes), o, "ScaleSession round trip");
    }
    {
        let o = WppObject::StoredMeasureAction(StoredMeasureAction {
            cmd: 1,
            rc: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredMeasureAction size");
        assert_eq!(
            WppObject::parse(276, &bytes),
            o,
            "StoredMeasureAction round trip"
        );
    }
    {
        let o = WppObject::StoredMeasureStatus(StoredMeasureStatus {
            cnt: 1,
            oldest_meas_time: 2,
            wifi_configured: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredMeasureStatus size");
        assert_eq!(
            WppObject::parse(277, &bytes),
            o,
            "StoredMeasureStatus round trip"
        );
    }
    {
        let o = WppObject::StoredMeasureMeta(StoredMeasureMeta {
            uid: 1,
            user_id_cnt: 2,
            user_id: vec![3, 4],
            attrib: 4,
            time: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredMeasureMeta size");
        assert_eq!(
            WppObject::parse(278, &bytes),
            o,
            "StoredMeasureMeta round trip"
        );
    }
    {
        let o = WppObject::StoredMeasureData(StoredMeasureData {
            value: 1,
            r#type: 2,
            exponent: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredMeasureData size");
        assert_eq!(
            WppObject::parse(279, &bytes),
            o,
            "StoredMeasureData round trip"
        );
    }
    {
        let o = WppObject::ConnectReason(ConnectReason {
            reason: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ConnectReason size");
        assert_eq!(WppObject::parse(280, &bytes), o, "ConnectReason round trip");
    }
    {
        let o = WppObject::UserUnit(UserUnit {
            cmd: 1,
            rc: 2,
            r#type: 3,
            unit: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "UserUnit size");
        assert_eq!(WppObject::parse(281, &bytes), o, "UserUnit round trip");
    }
    {
        let o = WppObject::UserInfo(UserInfo {
            firstname: String::from("wpp1"),
            user_id: 2,
            picto_id: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "UserInfo size");
        assert_eq!(WppObject::parse(282, &bytes), o, "UserInfo round trip");
    }
    {
        let o = WppObject::DblibDump(DblibDump {
            buf: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DblibDump size");
        assert_eq!(WppObject::parse(283, &bytes), o, "DblibDump round trip");
    }
    {
        let o = WppObject::AssocToken(AssocToken {
            token: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AssocToken size");
        assert_eq!(WppObject::parse(284, &bytes), o, "AssocToken round trip");
    }
    {
        let o = WppObject::DebugDumpData(DebugDumpData {
            buf: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DebugDumpData size");
        assert_eq!(WppObject::parse(285, &bytes), o, "DebugDumpData round trip");
    }
    {
        let o = WppObject::DebugDumpMask(DebugDumpMask {
            mask: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DebugDumpMask size");
        assert_eq!(WppObject::parse(286, &bytes), o, "DebugDumpMask round trip");
    }
    {
        let o = WppObject::DebugDumpType(DebugDumpType {
            r#type: 1,
            size: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DebugDumpType size");
        assert_eq!(WppObject::parse(287, &bytes), o, "DebugDumpType round trip");
    }
    {
        let o = WppObject::CommSupport(CommSupport {
            support: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CommSupport size");
        assert_eq!(WppObject::parse(288, &bytes), o, "CommSupport round trip");
    }
    {
        let o = WppObject::LocaleSet(LocaleSet {
            locale: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "LocaleSet size");
        assert_eq!(WppObject::parse(289, &bytes), o, "LocaleSet round trip");
    }
    {
        let o = WppObject::ProbeChallenge(ProbeChallenge {
            mac: String::from("wpp1"),
            challenge: vec![2, 3],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ProbeChallenge size");
        assert_eq!(
            WppObject::parse(290, &bytes),
            o,
            "ProbeChallenge round trip"
        );
    }
    {
        let o = WppObject::ProbeChallengeResponse(ProbeChallengeResponse {
            answer: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ProbeChallengeResponse size");
        assert_eq!(
            WppObject::parse(291, &bytes),
            o,
            "ProbeChallengeResponse round trip"
        );
    }
    {
        let o = WppObject::BatteryVoltageMv(BatteryVoltageMv {
            voltage_mv: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BatteryVoltageMv size");
        assert_eq!(
            WppObject::parse(292, &bytes),
            o,
            "BatteryVoltageMv round trip"
        );
    }
    {
        let o = WppObject::DebugDumpIgnoreData(DebugDumpIgnoreData {
            reason: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DebugDumpIgnoreData size");
        assert_eq!(
            WppObject::parse(293, &bytes),
            o,
            "DebugDumpIgnoreData round trip"
        );
    }
    {
        let o = WppObject::AlarmId(AlarmId {
            id: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AlarmId size");
        assert_eq!(WppObject::parse(294, &bytes), o, "AlarmId round trip");
    }
    {
        let o = WppObject::NbAlarm(NbAlarm {
            nb: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "NbAlarm size");
        assert_eq!(WppObject::parse(295, &bytes), o, "NbAlarm round trip");
    }
    {
        let o = WppObject::PlsReturnCode(PlsReturnCode {
            err: 1,
            total: 2,
            free: 3,
            used: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "PlsReturnCode size");
        assert_eq!(WppObject::parse(296, &bytes), o, "PlsReturnCode round trip");
    }
    {
        let o = WppObject::PlsStation(PlsStation {
            id: 1,
            timestamp: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "PlsStation size");
        assert_eq!(WppObject::parse(297, &bytes), o, "PlsStation round trip");
    }
    {
        let o = WppObject::AppProbe(AppProbe {
            os: 1,
            app: 2,
            version: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AppProbe size");
        assert_eq!(WppObject::parse(298, &bytes), o, "AppProbe round trip");
    }
    {
        let o = WppObject::StoredMeasureMetaExtend(StoredMeasureMetaExtend {
            algo: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredMeasureMetaExtend size");
        assert_eq!(
            WppObject::parse(299, &bytes),
            o,
            "StoredMeasureMetaExtend round trip"
        );
    }
    {
        let o = WppObject::FactoryState(FactoryState {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FactoryState size");
        assert_eq!(WppObject::parse(300, &bytes), o, "FactoryState round trip");
    }
    {
        let o = WppObject::DebugDumpFormat(DebugDumpFormat {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DebugDumpFormat size");
        assert_eq!(
            WppObject::parse(301, &bytes),
            o,
            "DebugDumpFormat round trip"
        );
    }
    {
        let o = WppObject::FactoryResetMode(FactoryResetMode {
            mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FactoryResetMode size");
        assert_eq!(
            WppObject::parse(302, &bytes),
            o,
            "FactoryResetMode round trip"
        );
    }
    {
        let o = WppObject::TrackerWearPos(TrackerWearPos {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TrackerWearPos size");
        assert_eq!(
            WppObject::parse(303, &bytes),
            o,
            "TrackerWearPos round trip"
        );
    }
    {
        let o = WppObject::CaptureSct01(CaptureSct01 {
            tamb: 1,
            tbody: vec![2, 3],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CaptureSct01 size");
        assert_eq!(WppObject::parse(304, &bytes), o, "CaptureSct01 round trip");
    }
    {
        let o = WppObject::FeatureMask(FeatureMask {
            mask: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FeatureMask size");
        assert_eq!(WppObject::parse(307, &bytes), o, "FeatureMask round trip");
    }
    {
        let o = WppObject::ScaleMedappUserInfo(ScaleMedappUserInfo {
            user_id: 1,
            height: 2,
            age: 3,
            sex: 4,
            fatmethod: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ScaleMedappUserInfo size");
        assert_eq!(
            WppObject::parse(308, &bytes),
            o,
            "ScaleMedappUserInfo round trip"
        );
    }
    {
        let o = WppObject::AccountKey(AccountKey {
            id: 1,
            secret: String::from("wpp2"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AccountKey size");
        assert_eq!(WppObject::parse(309, &bytes), o, "AccountKey round trip");
    }
    {
        let o = WppObject::AdvKey(AdvKey {
            secret: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AdvKey size");
        assert_eq!(WppObject::parse(310, &bytes), o, "AdvKey round trip");
    }
    {
        let o = WppObject::McuId(McuId {
            high: 1,
            low: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "McuId size");
        assert_eq!(WppObject::parse(311, &bytes), o, "McuId round trip");
    }
    {
        let o = WppObject::SensorId(SensorId {
            id: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SensorId size");
        assert_eq!(WppObject::parse(312, &bytes), o, "SensorId round trip");
    }
    {
        let o = WppObject::SoftdeviceVersion(SoftdeviceVersion {
            version: 1,
            subversion: 2,
            company_id: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SoftdeviceVersion size");
        assert_eq!(
            WppObject::parse(313, &bytes),
            o,
            "SoftdeviceVersion round trip"
        );
    }
    {
        let o = WppObject::HrAutoAlgo(HrAutoAlgo {
            mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "HrAutoAlgo size");
        assert_eq!(WppObject::parse(314, &bytes), o, "HrAutoAlgo round trip");
    }
    {
        let o = WppObject::RhTemp(RhTemp {
            temperature: 1,
            humidity: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "RhTemp size");
        assert_eq!(WppObject::parse(315, &bytes), o, "RhTemp round trip");
    }
    {
        let o = WppObject::WorkoutScreenList(WorkoutScreenList {
            screen_nb: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutScreenList size");
        assert_eq!(
            WppObject::parse(316, &bytes),
            o,
            "WorkoutScreenList round trip"
        );
    }
    {
        let o = WppObject::WorkoutScreenMetadata(WorkoutScreenMetadata {
            id: 1,
            version: 2,
            name: String::from("wpp3"),
            face_mode: 4,
            flag: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutScreenMetadata size");
        assert_eq!(
            WppObject::parse(317, &bytes),
            o,
            "WorkoutScreenMetadata round trip"
        );
    }
    {
        let o = WppObject::WorkoutScreenGlyphData(WorkoutScreenGlyphData {
            value: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutScreenGlyphData size");
        assert_eq!(
            WppObject::parse(318, &bytes),
            o,
            "WorkoutScreenGlyphData round trip"
        );
    }
    {
        let o = WppObject::WorkoutScreenName(WorkoutScreenName {
            value: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutScreenName size");
        assert_eq!(
            WppObject::parse(319, &bytes),
            o,
            "WorkoutScreenName round trip"
        );
    }
    {
        let o = WppObject::SyncRequest(SyncRequest {
            r#type: 1,
            reserved: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SyncRequest size");
        assert_eq!(WppObject::parse(320, &bytes), o, "SyncRequest round trip");
    }
    {
        let o = WppObject::WorkoutGpsStatus(WorkoutGpsStatus {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutGpsStatus size");
        assert_eq!(
            WppObject::parse(321, &bytes),
            o,
            "WorkoutGpsStatus round trip"
        );
    }
    {
        let o = WppObject::Silent(Silent {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Silent size");
        assert_eq!(WppObject::parse(322, &bytes), o, "Silent round trip");
    }
    {
        let o = WppObject::StoredSignalMeta(StoredSignalMeta {
            r#type: 1,
            sampling_freq: 2,
            format: 3,
            size: 4,
            resolution: 5,
            channel: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredSignalMeta size");
        assert_eq!(
            WppObject::parse(323, &bytes),
            o,
            "StoredSignalMeta round trip"
        );
    }
    {
        let o = WppObject::StoredSignalData(StoredSignalData {
            samples: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredSignalData size");
        assert_eq!(
            WppObject::parse(324, &bytes),
            o,
            "StoredSignalData round trip"
        );
    }
    {
        let o = WppObject::Id(Id {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Id size");
        assert_eq!(WppObject::parse(325, &bytes), o, "Id round trip");
    }
    {
        let o = WppObject::StoredSignalMetaExtend(StoredSignalMetaExtend {
            duration: 1,
            total_size: 2,
            filter_bank: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StoredSignalMetaExtend size");
        assert_eq!(
            WppObject::parse(326, &bytes),
            o,
            "StoredSignalMetaExtend round trip"
        );
    }
    {
        let o = WppObject::UnitConversionParameters(UnitConversionParameters {
            offset: 1,
            gain: 2,
            qfix: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "UnitConversionParameters size");
        assert_eq!(
            WppObject::parse(327, &bytes),
            o,
            "UnitConversionParameters round trip"
        );
    }
    {
        let o = WppObject::CustoScreenMetadata(CustoScreenMetadata {
            userid: 1,
            start_ts: 2,
            end_ts: 3,
            id: 4,
            display_style: 5,
            msg: String::from("wpp6"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CustoScreenMetadata size");
        assert_eq!(
            WppObject::parse(328, &bytes),
            o,
            "CustoScreenMetadata round trip"
        );
    }
    {
        let o = WppObject::AlgorithmVersion(AlgorithmVersion {
            r#type: 1,
            version: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AlgorithmVersion size");
        assert_eq!(
            WppObject::parse(329, &bytes),
            o,
            "AlgorithmVersion round trip"
        );
    }
    {
        let o = WppObject::FirmwareVersion(FirmwareVersion {
            version: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FirmwareVersion size");
        assert_eq!(
            WppObject::parse(330, &bytes),
            o,
            "FirmwareVersion round trip"
        );
    }
    {
        let o = WppObject::RawDataCmd(RawDataCmd {
            cmd: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "RawDataCmd size");
        assert_eq!(WppObject::parse(331, &bytes), o, "RawDataCmd round trip");
    }
    {
        let o = WppObject::SignalAttribut(SignalAttribut {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SignalAttribut size");
        assert_eq!(
            WppObject::parse(332, &bytes),
            o,
            "SignalAttribut round trip"
        );
    }
    {
        let o = WppObject::WifiAnt(WifiAnt {
            flags: 1,
            txant: 2,
            rxant: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WifiAnt size");
        assert_eq!(WppObject::parse(513, &bytes), o, "WifiAnt round trip");
    }
    {
        let o = WppObject::Backlight(Backlight {
            cmd: 1,
            value: 2,
            rc: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Backlight size");
        assert_eq!(WppObject::parse(514, &bytes), o, "Backlight round trip");
    }
    {
        let o = WppObject::Lcd(Lcd {
            cmd: 1,
            value: 2,
            rc: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Lcd size");
        assert_eq!(WppObject::parse(515, &bytes), o, "Lcd round trip");
    }
    {
        let o = WppObject::Gpio(Gpio {
            cmd: 1,
            bank: 2,
            gpio: 3,
            mode: 4,
            state: 5,
            rc: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Gpio size");
        assert_eq!(WppObject::parse(516, &bytes), o, "Gpio round trip");
    }
    {
        let o = WppObject::Perso(Perso {
            cmd: 1,
            key: String::from("wpp2"),
            val1: String::from("wpp3"),
            val2: String::from("wpp4"),
            val3: String::from("wpp5"),
            rc: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Perso size");
        assert_eq!(WppObject::parse(517, &bytes), o, "Perso round trip");
    }
    {
        let o = WppObject::Uint32(Uint32 {
            val: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Uint32 size");
        assert_eq!(WppObject::parse(518, &bytes), o, "Uint32 round trip");
    }
    {
        let o = WppObject::WppString(WppString {
            val: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WppString size");
        assert_eq!(WppObject::parse(519, &bytes), o, "WppString round trip");
    }
    {
        let o = WppObject::Audiotest(Audiotest {
            cmd: 1,
            volume1: 2,
            volume2: 3,
            result: 4,
            rc: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Audiotest size");
        assert_eq!(WppObject::parse(520, &bytes), o, "Audiotest round trip");
    }
    {
        let o = WppObject::Weighttest(Weighttest {
            cmd: 1,
            adc: 2,
            dac: 3,
            time: 4,
            noise_mean: 5,
            noise_peak: 6,
            delta_max: 7,
            rc: 8,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Weighttest size");
        assert_eq!(WppObject::parse(521, &bytes), o, "Weighttest round trip");
    }
    {
        let o = WppObject::Adc(Adc {
            cmd: 1,
            bank: 2,
            adc: 3,
            val: 4,
            sample: 5,
            rc: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Adc size");
        assert_eq!(WppObject::parse(522, &bytes), o, "Adc round trip");
    }
    {
        let o = WppObject::Dac(Dac {
            cmd: 1,
            bank: 2,
            dac: 3,
            val: 4,
            rc: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Dac size");
        assert_eq!(WppObject::parse(523, &bytes), o, "Dac round trip");
    }
    {
        let o = WppObject::WeightCal(WeightCal {
            factory_g: 1,
            dac: vec![2, 3],
            cell50: vec![3, 4],
            cell100: vec![4, 5],
            cell150: vec![5, 6],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WeightCal size");
        assert_eq!(WppObject::parse(524, &bytes), o, "WeightCal round trip");
    }
    {
        let o = WppObject::Wl(Wl {
            rc: 1,
            cmd: 2,
            set: 3,
            buf: String::from("wpp4"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Wl size");
        assert_eq!(WppObject::parse(525, &bytes), o, "Wl round trip");
    }
    {
        let o = WppObject::Zmeter(Zmeter {
            rc: 1,
            cmd: 2,
            re: 3,
            ri: 4,
            mod_z: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Zmeter size");
        assert_eq!(WppObject::parse(526, &bytes), o, "Zmeter round trip");
    }
    {
        let o = WppObject::ZmeterCal(ZmeterCal {
            index: 1,
            freq: 2,
            i_cr: vec![3, 4],
            u_cr: vec![4, 5],
            ph_cr: vec![5, 6],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ZmeterCal size");
        assert_eq!(WppObject::parse(527, &bytes), o, "ZmeterCal round trip");
    }
    {
        let o = WppObject::Spiflash(Spiflash {
            cmd: 1,
            rc: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Spiflash size");
        assert_eq!(WppObject::parse(528, &bytes), o, "Spiflash round trip");
    }
    {
        let o = WppObject::Rtc(Rtc {
            cmd: 1,
            rc: 2,
            val: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Rtc size");
        assert_eq!(WppObject::parse(529, &bytes), o, "Rtc round trip");
    }
    {
        let o = WppObject::WeightVerif(WeightVerif {
            z_weight: 1,
            weight: vec![2, 3],
            re: 3,
            ri: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WeightVerif size");
        assert_eq!(WppObject::parse(530, &bytes), o, "WeightVerif round trip");
    }
    {
        let o = WppObject::Dump(Dump {
            dev: 1,
            offset: 2,
            len: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Dump size");
        assert_eq!(WppObject::parse(531, &bytes), o, "Dump round trip");
    }
    {
        let o = WppObject::Selftest(Selftest {
            dev: 1,
            rc: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Selftest size");
        assert_eq!(WppObject::parse(532, &bytes), o, "Selftest round trip");
    }
    {
        let o = WppObject::Slot(Slot {
            cmd: 1,
            slot: 2,
            reserved: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Slot size");
        assert_eq!(WppObject::parse(533, &bytes), o, "Slot round trip");
    }
    {
        let o = WppObject::IapWci(IapWci {
            status: 1,
            security: 2,
            ssid: String::from("wpp3"),
            passphrase: String::from("wpp4"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "IapWci size");
        assert_eq!(WppObject::parse(534, &bytes), o, "IapWci round trip");
    }
    {
        let o = WppObject::Int32(Int32 {
            val: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Int32 size");
        assert_eq!(WppObject::parse(535, &bytes), o, "Int32 round trip");
    }
    {
        let o = WppObject::Uint64(Uint64 {
            val: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Uint64 size");
        assert_eq!(WppObject::parse(536, &bytes), o, "Uint64 round trip");
    }
    {
        let o = WppObject::UpdateFirmware(UpdateFirmware {
            fw_type: 1,
            fw_size: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "UpdateFirmware size");
        assert_eq!(
            WppObject::parse(1025, &bytes),
            o,
            "UpdateFirmware round trip"
        );
    }
    {
        let o = WppObject::NetupdateProgress(NetupdateProgress {
            percent: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "NetupdateProgress size");
        assert_eq!(
            WppObject::parse(1040, &bytes),
            o,
            "NetupdateProgress round trip"
        );
    }
    {
        let o = WppObject::NetupdateResult(NetupdateResult {
            rc: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "NetupdateResult size");
        assert_eq!(
            WppObject::parse(1041, &bytes),
            o,
            "NetupdateResult round trip"
        );
    }
    {
        let o = WppObject::Mac(Mac {
            mac: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Mac size");
        assert_eq!(WppObject::parse(1043, &bytes), o, "Mac round trip");
    }
    {
        let o = WppObject::TimeSet(TimeSet {
            utc: 1,
            gmt_offset: 2,
            dst_change_time: 3,
            next_gmt_offset: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TimeSet size");
        assert_eq!(WppObject::parse(1281, &bytes), o, "TimeSet round trip");
    }
    {
        let o = WppObject::TimeSetReply(TimeSetReply {
            drift: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TimeSetReply size");
        assert_eq!(WppObject::parse(1282, &bytes), o, "TimeSetReply round trip");
    }
    {
        let o = WppObject::TrackerUser(TrackerUser {
            id: 1,
            weight: 2,
            height: 3,
            gender: 4,
            birth: 5,
            first_name: String::from("wpp6"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TrackerUser size");
        assert_eq!(WppObject::parse(1283, &bytes), o, "TrackerUser round trip");
    }
    {
        let o = WppObject::BatteryStatus(BatteryStatus {
            battery_percent: 1,
            battery_state: 2,
            battery_mv: 3,
            reserved: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BatteryStatus size");
        assert_eq!(
            WppObject::parse(1284, &bytes),
            o,
            "BatteryStatus round trip"
        );
    }
    {
        let o = WppObject::WamDisplayedInfo(WamDisplayedInfo {
            steps: 1,
            distance: 2,
            ascent: 3,
            descent: 4,
            calories: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamDisplayedInfo size");
        assert_eq!(
            WppObject::parse(1285, &bytes),
            o,
            "WamDisplayedInfo round trip"
        );
    }
    {
        let o = WppObject::WamVasistasGet(WamVasistasGet {
            utc_start: 1,
            max: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasGet size");
        assert_eq!(
            WppObject::parse(1286, &bytes),
            o,
            "WamVasistasGet round trip"
        );
    }
    {
        let o = WppObject::WamDailyActivities(WamDailyActivities {
            steps: 1,
            distance: 2,
            ascent: 3,
            descent: 4,
            calories: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamDailyActivities size");
        assert_eq!(
            WppObject::parse(1287, &bytes),
            o,
            "WamDailyActivities round trip"
        );
    }
    {
        let o = WppObject::WamRawData(WamRawData {
            buf: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamRawData size");
        assert_eq!(WppObject::parse(1288, &bytes), o, "WamRawData round trip");
    }
    {
        let o = WppObject::WamScreensList(WamScreensList {
            screen_numbers: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamScreensList size");
        assert_eq!(
            WppObject::parse(1289, &bytes),
            o,
            "WamScreensList round trip"
        );
    }
    {
        let o = WppObject::WamAutoSleep(WamAutoSleep {
            auto_sleep: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamAutoSleep size");
        assert_eq!(WppObject::parse(1290, &bytes), o, "WamAutoSleep round trip");
    }
    {
        let o = WppObject::AlarmSetSimple(AlarmSetSimple {
            hour: 1,
            minute: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AlarmSetSimple size");
        assert_eq!(
            WppObject::parse(1291, &bytes),
            o,
            "AlarmSetSimple round trip"
        );
    }
    {
        let o = WppObject::HandsMove(HandsMove {
            hand: 1,
            nb_steps: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "HandsMove size");
        assert_eq!(WppObject::parse(1292, &bytes), o, "HandsMove round trip");
    }
    {
        let o = WppObject::DemoStart(DemoStart {
            r#type: 1,
            timestamp: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DemoStart size");
        assert_eq!(WppObject::parse(1293, &bytes), o, "DemoStart round trip");
    }
    {
        let o = WppObject::SetTime(SetTime {
            utc: 1,
            gmt_offset: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SetTime size");
        assert_eq!(WppObject::parse(1296, &bytes), o, "SetTime round trip");
    }
    {
        let o = WppObject::TrackerGoal(TrackerGoal {
            goal_type: 1,
            value: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TrackerGoal size");
        assert_eq!(WppObject::parse(1297, &bytes), o, "TrackerGoal round trip");
    }
    {
        let o = WppObject::Alarm(Alarm {
            hour: 1,
            minute: 2,
            wday: 3,
            mday: 4,
            month: 5,
            year: 6,
            span: 7,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Alarm size");
        assert_eq!(WppObject::parse(1298, &bytes), o, "Alarm round trip");
    }
    {
        let o = WppObject::UserSecret(UserSecret {
            user_secret: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "UserSecret size");
        assert_eq!(WppObject::parse(1299, &bytes), o, "UserSecret round trip");
    }
    {
        let o = WppObject::Name(Name {
            str: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Name size");
        assert_eq!(WppObject::parse(1300, &bytes), o, "Name round trip");
    }
    {
        let o = WppObject::VasistasType(VasistasType {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasType size");
        assert_eq!(WppObject::parse(1301, &bytes), o, "VasistasType round trip");
    }
    {
        let o = WppObject::ScreenList(ScreenList {
            id: 1,
            userid: 2,
            start_ts: 3,
            end_ts: 4,
            embedded_id: 5,
            source: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ScreenList size");
        assert_eq!(WppObject::parse(1302, &bytes), o, "ScreenList round trip");
    }
    {
        let o = WppObject::WamVasistasHead(WamVasistasHead {
            utc: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasHead size");
        assert_eq!(
            WppObject::parse(1537, &bytes),
            o,
            "WamVasistasHead round trip"
        );
    }
    {
        let o = WppObject::WamVasistasDuration(WamVasistasDuration {
            duration: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasDuration size");
        assert_eq!(
            WppObject::parse(1538, &bytes),
            o,
            "WamVasistasDuration round trip"
        );
    }
    {
        let o = WppObject::WamVasistasAwake(WamVasistasAwake {
            steps: 1,
            distance: 2,
            ascent: 3,
            descent: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasAwake size");
        assert_eq!(
            WppObject::parse(1539, &bytes),
            o,
            "WamVasistasAwake round trip"
        );
    }
    {
        let o = WppObject::WamVasistasWalk(WamVasistasWalk {
            level: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasWalk size");
        assert_eq!(
            WppObject::parse(1540, &bytes),
            o,
            "WamVasistasWalk round trip"
        );
    }
    {
        let o = WppObject::WamVasistasRun(WamVasistasRun {
            level: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasRun size");
        assert_eq!(
            WppObject::parse(1541, &bytes),
            o,
            "WamVasistasRun round trip"
        );
    }
    {
        let o = WppObject::WamVasistasSleepDbg(WamVasistasSleepDbg {
            level: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasSleepDbg size");
        assert_eq!(
            WppObject::parse(1542, &bytes),
            o,
            "WamVasistasSleepDbg round trip"
        );
    }
    {
        let o = WppObject::WamVasistasSleep(WamVasistasSleep {
            level: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasSleep size");
        assert_eq!(
            WppObject::parse(1543, &bytes),
            o,
            "WamVasistasSleep round trip"
        );
    }
    {
        let o = WppObject::WamVasistasMetCal(WamVasistasMetCal {
            calories: 1,
            met: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasMetCal size");
        assert_eq!(
            WppObject::parse(1544, &bytes),
            o,
            "WamVasistasMetCal round trip"
        );
    }
    {
        let o = WppObject::VasistasSwimV1(VasistasSwimV1 {
            mvt: 1,
            laps: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasSwimV1 size");
        assert_eq!(
            WppObject::parse(1545, &bytes),
            o,
            "VasistasSwimV1 round trip"
        );
    }
    {
        let o = WppObject::WamVasistasMetCalEarned(WamVasistasMetCalEarned {
            calories: 1,
            met: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WamVasistasMetCalEarned size");
        assert_eq!(
            WppObject::parse(1546, &bytes),
            o,
            "WamVasistasMetCalEarned round trip"
        );
    }
    {
        let o = WppObject::VasistasActiRecoV1V2(VasistasActiRecoV1V2 {
            reco_v1: 1,
            reco_v2: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasActiRecoV1V2 size");
        assert_eq!(
            WppObject::parse(1547, &bytes),
            o,
            "VasistasActiRecoV1V2 round trip"
        );
    }
    {
        let o = WppObject::VasistasUncertainSwim(VasistasUncertainSwim {
            is_run: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasUncertainSwim size");
        assert_eq!(
            WppObject::parse(1548, &bytes),
            o,
            "VasistasUncertainSwim round trip"
        );
    }
    {
        let o = WppObject::VasistasSwimType(VasistasSwimType {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasSwimType size");
        assert_eq!(
            WppObject::parse(1549, &bytes),
            o,
            "VasistasSwimType round trip"
        );
    }
    {
        let o = WppObject::WpmSetMode(WpmSetMode {
            mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmSetMode size");
        assert_eq!(WppObject::parse(1888, &bytes), o, "WpmSetMode round trip");
    }
    {
        let o = WppObject::WpmStart(WpmStart {
            start: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmStart size");
        assert_eq!(WppObject::parse(1889, &bytes), o, "WpmStart round trip");
    }
    {
        let o = WppObject::WpmResult(WpmResult {
            err: 1,
            sys: 2,
            dia: 3,
            pul: 4,
            pmax: 5,
            pmin: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmResult size");
        assert_eq!(WppObject::parse(1892, &bytes), o, "WpmResult round trip");
    }
    {
        let o = WppObject::WpmBpPulse(WpmBpPulse {
            pressure: 1,
            energy: 2,
            qwave: 3,
            time: 4,
            amp: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmBpPulse size");
        assert_eq!(WppObject::parse(1893, &bytes), o, "WpmBpPulse round trip");
    }
    {
        let o = WppObject::WpmEvent(WpmEvent {
            event: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmEvent size");
        assert_eq!(WppObject::parse(1894, &bytes), o, "WpmEvent round trip");
    }
    {
        let o = WppObject::WpmSetValve(WpmSetValve {
            valve_state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmSetValve size");
        assert_eq!(WppObject::parse(1911, &bytes), o, "WpmSetValve round trip");
    }
    {
        let o = WppObject::WpmSetMotor(WpmSetMotor {
            motor_state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmSetMotor size");
        assert_eq!(WppObject::parse(1912, &bytes), o, "WpmSetMotor round trip");
    }
    {
        let o = WppObject::WpmGetPressure(WpmGetPressure {
            pressure: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmGetPressure size");
        assert_eq!(
            WppObject::parse(1913, &bytes),
            o,
            "WpmGetPressure round trip"
        );
    }
    {
        let o = WppObject::WpmGetzero(WpmGetzero {
            pressure_adc: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmGetzero size");
        assert_eq!(WppObject::parse(1917, &bytes), o, "WpmGetzero round trip");
    }
    {
        let o = WppObject::WpmPressure(WpmPressure {
            time: 1,
            pression: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WpmPressure size");
        assert_eq!(WppObject::parse(1923, &bytes), o, "WpmPressure round trip");
    }
    {
        let o = WppObject::WsmMode(WsmMode {
            mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmMode size");
        assert_eq!(WppObject::parse(2048, &bytes), o, "WsmMode round trip");
    }
    {
        let o = WppObject::WsmValve(WsmValve {
            valve_state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmValve size");
        assert_eq!(WppObject::parse(2049, &bytes), o, "WsmValve round trip");
    }
    {
        let o = WppObject::WsmMotor(WsmMotor {
            motor_state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmMotor size");
        assert_eq!(WppObject::parse(2050, &bytes), o, "WsmMotor round trip");
    }
    {
        let o = WppObject::WsmPressureMvtGet(WsmPressureMvtGet {
            pressure: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmPressureMvtGet size");
        assert_eq!(
            WppObject::parse(2051, &bytes),
            o,
            "WsmPressureMvtGet round trip"
        );
    }
    {
        let o = WppObject::WsmZeroGet(WsmZeroGet {
            pressure_adc: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmZeroGet size");
        assert_eq!(WppObject::parse(2052, &bytes), o, "WsmZeroGet round trip");
    }
    {
        let o = WppObject::WsmUser(WsmUser {
            id: 1,
            weight: 2,
            height: 3,
            gender: 4,
            birth: 5,
            first_name: String::from("wpp6"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmUser size");
        assert_eq!(WppObject::parse(2055, &bytes), o, "WsmUser round trip");
    }
    {
        let o = WppObject::WsmVasistasGet(WsmVasistasGet {
            utc_start: 1,
            max: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmVasistasGet size");
        assert_eq!(
            WppObject::parse(2056, &bytes),
            o,
            "WsmVasistasGet round trip"
        );
    }
    {
        let o = WppObject::WsmRawDataGet(WsmRawDataGet {
            buf: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmRawDataGet size");
        assert_eq!(
            WppObject::parse(2057, &bytes),
            o,
            "WsmRawDataGet round trip"
        );
    }
    {
        let o = WppObject::WsmLedControl(WsmLedControl {
            id: 1,
            delay: 2,
            timeout: 3,
            red: 4,
            green: 5,
            blue: 6,
            brightness: 7,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmLedControl size");
        assert_eq!(
            WppObject::parse(2058, &bytes),
            o,
            "WsmLedControl round trip"
        );
    }
    {
        let o = WppObject::WsmPrivateMode(WsmPrivateMode {
            private_mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmPrivateMode size");
        assert_eq!(
            WppObject::parse(2059, &bytes),
            o,
            "WsmPrivateMode round trip"
        );
    }
    {
        let o = WppObject::WsmVasistasGeneral(WsmVasistasGeneral {
            utc: 1,
            presence: 2,
            sleep_stage: 3,
            hr_median: 4,
            hr_variability: 5,
            rr_median: 6,
            rr_amplitude_max: 7,
            rr_amplitude_min: 8,
            rr_frequency_max: 9,
            rr_frequency_min: 10,
            value1: 11,
            value2: 12,
            value3: 13,
            value4: 14,
            value5: 15,
            value6: 16,
            value7: 17,
            value8: 18,
            value9: 19,
            value10: 20,
            value11: 21,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmVasistasGeneral size");
        assert_eq!(
            WppObject::parse(2066, &bytes),
            o,
            "WsmVasistasGeneral round trip"
        );
    }
    {
        let o = WppObject::WsmVasistasGeneralV2(WsmVasistasGeneralV2 {
            utc: 1,
            presence: 2,
            sleep_stage: 3,
            hr_median: 4,
            rr_median: 5,
            value1: 6,
            value2: 7,
            value3: 8,
            value4: 9,
            value5: 10,
            value6: 11,
            value7: 12,
            value8: 13,
            value9: 14,
            value10: 15,
            value11: 16,
            value12: 17,
            value13: 18,
            value14: 19,
            value15: 20,
            value16: 21,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsmVasistasGeneralV2 size");
        assert_eq!(
            WppObject::parse(2067, &bytes),
            o,
            "WsmVasistasGeneralV2 round trip"
        );
    }
    {
        let o = WppObject::WsdScanWsmResult(WsdScanWsmResult {
            vid: 1,
            pid: 2,
            name: String::from("wpp3"),
            mac: String::from("wpp4"),
            secret: String::from("wpp5"),
            hard_version: 6,
            mfg_id: String::from("wpp7"),
            bl_version: 8,
            soft_version: 9,
            rescue_version: 10,
            userid: 11,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdScanWsmResult size");
        assert_eq!(
            WppObject::parse(2305, &bytes),
            o,
            "WsdScanWsmResult round trip"
        );
    }
    {
        let o = WppObject::WsdScanWsmFinish(WsdScanWsmFinish {
            count: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdScanWsmFinish size");
        assert_eq!(
            WppObject::parse(2306, &bytes),
            o,
            "WsdScanWsmFinish round trip"
        );
    }
    {
        let o = WppObject::WsdWsmUser(WsdWsmUser {
            id: 1,
            weight: 2,
            height: 3,
            gender: 4,
            birth: 5,
            first_name: String::from("wpp6"),
            mac: String::from("wpp7"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdWsmUser size");
        assert_eq!(WppObject::parse(2307, &bytes), o, "WsdWsmUser round trip");
    }
    {
        let o = WppObject::WsdAlarm(WsdAlarm {
            hour: 1,
            minute: 2,
            day: 3,
            volume: 4,
            brightness: 5,
            program: 6,
            state: 7,
            span: 8,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdAlarm size");
        assert_eq!(WppObject::parse(2308, &bytes), o, "WsdAlarm round trip");
    }
    {
        let o = WppObject::WsdSetWsmUserResult(WsdSetWsmUserResult {
            rc: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdSetWsmUserResult size");
        assert_eq!(
            WppObject::parse(2309, &bytes),
            o,
            "WsdSetWsmUserResult round trip"
        );
    }
    {
        let o = WppObject::WsdProgramSettings(WsdProgramSettings {
            volume: 1,
            brightness: 2,
            r#type: 3,
            id: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdProgramSettings size");
        assert_eq!(
            WppObject::parse(2310, &bytes),
            o,
            "WsdProgramSettings round trip"
        );
    }
    {
        let o = WppObject::WsdStatus(WsdStatus {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdStatus size");
        assert_eq!(WppObject::parse(2311, &bytes), o, "WsdStatus round trip");
    }
    {
        let o = WppObject::WsdProgramId(WsdProgramId {
            r#type: 1,
            id: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdProgramId size");
        assert_eq!(WppObject::parse(2312, &bytes), o, "WsdProgramId round trip");
    }
    {
        let o = WppObject::WsdProgramInfo(WsdProgramInfo {
            r#type: 1,
            id: 2,
            selected: 3,
            available: 4,
            progress: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdProgramInfo size");
        assert_eq!(
            WppObject::parse(2313, &bytes),
            o,
            "WsdProgramInfo round trip"
        );
    }
    {
        let o = WppObject::SetClockMode(SetClockMode {
            r#type: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SetClockMode size");
        assert_eq!(WppObject::parse(2314, &bytes), o, "SetClockMode round trip");
    }
    {
        let o = WppObject::Color(Color {
            red: 1,
            green: 2,
            blue: 3,
            white: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Color size");
        assert_eq!(WppObject::parse(2315, &bytes), o, "Color round trip");
    }
    {
        let o = WppObject::LampState(LampState {
            state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "LampState size");
        assert_eq!(WppObject::parse(2316, &bytes), o, "LampState round trip");
    }
    {
        let o = WppObject::ClockDisplaySetting(ClockDisplaySetting {
            state: 1,
            level: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ClockDisplaySetting size");
        assert_eq!(
            WppObject::parse(2317, &bytes),
            o,
            "ClockDisplaySetting round trip"
        );
    }
    {
        let o = WppObject::BleLinkStatus(BleLinkStatus {
            mode: 1,
            request: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BleLinkStatus size");
        assert_eq!(
            WppObject::parse(2318, &bytes),
            o,
            "BleLinkStatus round trip"
        );
    }
    {
        let o = WppObject::WsdSubstatus(WsdSubstatus {
            substatus: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdSubstatus size");
        assert_eq!(WppObject::parse(2319, &bytes), o, "WsdSubstatus round trip");
    }
    {
        let o = WppObject::WppEvtSupport(WppEvtSupport {
            enable: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WppEvtSupport size");
        assert_eq!(
            WppObject::parse(2320, &bytes),
            o,
            "WppEvtSupport round trip"
        );
    }
    {
        let o = WppObject::SwimPoolSize(SwimPoolSize {
            size: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SwimPoolSize size");
        assert_eq!(WppObject::parse(2321, &bytes), o, "SwimPoolSize round trip");
    }
    {
        let o = WppObject::ColorHsl(ColorHsl {
            hue: 1,
            saturation: 2,
            luminosity: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ColorHsl size");
        assert_eq!(WppObject::parse(2322, &bytes), o, "ColorHsl round trip");
    }
    {
        let o = WppObject::ColorHsv(ColorHsv {
            hue: 1,
            saturation: 2,
            value: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ColorHsv size");
        assert_eq!(WppObject::parse(2323, &bytes), o, "ColorHsv round trip");
    }
    {
        let o = WppObject::ColorRgb(ColorRgb {
            red: 1,
            green: 2,
            blue: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ColorRgb size");
        assert_eq!(WppObject::parse(2324, &bytes), o, "ColorRgb round trip");
    }
    {
        let o = WppObject::ResponsiveLightMode(ResponsiveLightMode {
            mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ResponsiveLightMode size");
        assert_eq!(
            WppObject::parse(2325, &bytes),
            o,
            "ResponsiveLightMode round trip"
        );
    }
    {
        let o = WppObject::ProgramSetting(ProgramSetting {
            setting: 1,
            data: String::from("wpp2"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ProgramSetting size");
        assert_eq!(
            WppObject::parse(2326, &bytes),
            o,
            "ProgramSetting round trip"
        );
    }
    {
        let o = WppObject::ProgramType(ProgramType {
            program: 1,
            sub_program: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ProgramType size");
        assert_eq!(WppObject::parse(2327, &bytes), o, "ProgramType round trip");
    }
    {
        let o = WppObject::ReturnCode(ReturnCode {
            rc: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ReturnCode size");
        assert_eq!(WppObject::parse(2328, &bytes), o, "ReturnCode round trip");
    }
    {
        let o = WppObject::AlarmEnabled(AlarmEnabled {
            state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AlarmEnabled size");
        assert_eq!(WppObject::parse(2329, &bytes), o, "AlarmEnabled round trip");
    }
    {
        let o = WppObject::TappingStatus(TappingStatus {
            state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TappingStatus size");
        assert_eq!(
            WppObject::parse(2330, &bytes),
            o,
            "TappingStatus round trip"
        );
    }
    {
        let o = WppObject::SwimStatus(SwimStatus {
            state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SwimStatus size");
        assert_eq!(WppObject::parse(2331, &bytes), o, "SwimStatus round trip");
    }
    {
        let o = WppObject::CalibrationSession(CalibrationSession {
            param0: 1,
            param1: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CalibrationSession size");
        assert_eq!(
            WppObject::parse(2332, &bytes),
            o,
            "CalibrationSession round trip"
        );
    }
    {
        let o = WppObject::WsdSubprogram(WsdSubprogram {
            subprogram: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WsdSubprogram size");
        assert_eq!(
            WppObject::parse(2333, &bytes),
            o,
            "WsdSubprogram round trip"
        );
    }
    {
        let o = WppObject::SendEnvMeas(SendEnvMeas {
            userid: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SendEnvMeas size");
        assert_eq!(WppObject::parse(2338, &bytes), o, "SendEnvMeas round trip");
    }
    {
        let o = WppObject::DispPrefs0(DispPrefs0 {
            gui: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DispPrefs0 size");
        assert_eq!(WppObject::parse(2340, &bytes), o, "DispPrefs0 round trip");
    }
    {
        let o = WppObject::DispBehavior0(DispBehavior0 {
            behavior: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DispBehavior0 size");
        assert_eq!(
            WppObject::parse(2341, &bytes),
            o,
            "DispBehavior0 round trip"
        );
    }
    {
        let o = WppObject::HrAs7000Stop(HrAs7000Stop {
            stop: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "HrAs7000Stop size");
        assert_eq!(WppObject::parse(2342, &bytes), o, "HrAs7000Stop round trip");
    }
    {
        let o = WppObject::HrAs7000(HrAs7000 {
            tid: 1,
            heartrate: 2,
            quality: 3,
            led_current: 4,
            ntc_raw: 5,
            gsr: 6,
            nrf_tmp: 7,
            adxl_tmp: 8,
            tia_avg: 9,
            ofe_avg: 10,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "HrAs7000 size");
        assert_eq!(WppObject::parse(2343, &bytes), o, "HrAs7000 round trip");
    }
    {
        let o = WppObject::AppProbeOsVersion(AppProbeOsVersion {
            os_version: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AppProbeOsVersion size");
        assert_eq!(
            WppObject::parse(2344, &bytes),
            o,
            "AppProbeOsVersion round trip"
        );
    }
    {
        let o = WppObject::VasistasHeartrate(VasistasHeartrate {
            heartrate: 1,
            quality: 2,
            temperature: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasHeartrate size");
        assert_eq!(
            WppObject::parse(2345, &bytes),
            o,
            "VasistasHeartrate round trip"
        );
    }
    {
        let o = WppObject::AncsStatus(AncsStatus {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AncsStatus size");
        assert_eq!(WppObject::parse(2346, &bytes), o, "AncsStatus round trip");
    }
    {
        let o = WppObject::AncsConfig(AncsConfig {
            r#type: 1,
            status: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AncsConfig size");
        assert_eq!(WppObject::parse(2347, &bytes), o, "AncsConfig round trip");
    }
    {
        let o = WppObject::VasistasActivityStatus(VasistasActivityStatus {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasActivityStatus size");
        assert_eq!(
            WppObject::parse(2348, &bytes),
            o,
            "VasistasActivityStatus round trip"
        );
    }
    {
        let o = WppObject::TempCalSet(TempCalSet {
            point_idx: 1,
            temp_mili: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TempCalSet size");
        assert_eq!(WppObject::parse(2349, &bytes), o, "TempCalSet round trip");
    }
    {
        let o = WppObject::TempCalReply(TempCalReply {
            point_idx: 1,
            temp_mili: 2,
            raw_value: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TempCalReply size");
        assert_eq!(WppObject::parse(2350, &bytes), o, "TempCalReply round trip");
    }
    {
        let o = WppObject::CalibrationType(CalibrationType {
            r#type: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CalibrationType size");
        assert_eq!(
            WppObject::parse(2351, &bytes),
            o,
            "CalibrationType round trip"
        );
    }
    {
        let o = WppObject::CalibrationPoint(CalibrationPoint {
            point_idx: 1,
            reference: 2,
            value: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CalibrationPoint size");
        assert_eq!(
            WppObject::parse(2352, &bytes),
            o,
            "CalibrationPoint round trip"
        );
    }
    {
        let o = WppObject::VasistasWristHr(VasistasWristHr {
            heartrate: 1,
            quality: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasWristHr size");
        assert_eq!(
            WppObject::parse(2353, &bytes),
            o,
            "VasistasWristHr round trip"
        );
    }
    {
        let o = WppObject::VasistasWristTemp(VasistasWristTemp {
            temperature: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasWristTemp size");
        assert_eq!(
            WppObject::parse(2354, &bytes),
            o,
            "VasistasWristTemp round trip"
        );
    }
    {
        let o = WppObject::HomeScreen(HomeScreen {
            id: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "HomeScreen size");
        assert_eq!(WppObject::parse(2355, &bytes), o, "HomeScreen round trip");
    }
    {
        let o = WppObject::LightSensorReply(LightSensorReply {
            value: 1,
            r#type: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "LightSensorReply size");
        assert_eq!(
            WppObject::parse(2356, &bytes),
            o,
            "LightSensorReply round trip"
        );
    }
    {
        let o = WppObject::CustomizationIdGet(CustomizationIdGet {
            identifier: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CustomizationIdGet size");
        assert_eq!(
            WppObject::parse(2357, &bytes),
            o,
            "CustomizationIdGet round trip"
        );
    }
    {
        let o = WppObject::CustomizationIdSet(CustomizationIdSet {
            identifier: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CustomizationIdSet size");
        assert_eq!(
            WppObject::parse(2358, &bytes),
            o,
            "CustomizationIdSet round trip"
        );
    }
    {
        let o = WppObject::LuminosityLevel(LuminosityLevel {
            mode: 1,
            level: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "LuminosityLevel size");
        assert_eq!(
            WppObject::parse(2359, &bytes),
            o,
            "LuminosityLevel round trip"
        );
    }
    {
        let o = WppObject::AnsStatus(AnsStatus {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AnsStatus size");
        assert_eq!(WppObject::parse(2360, &bytes), o, "AnsStatus round trip");
    }
    {
        let o = WppObject::AnsConfig(AnsConfig {
            r#type: 1,
            status: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AnsConfig size");
        assert_eq!(WppObject::parse(2361, &bytes), o, "AnsConfig round trip");
    }
    {
        let o = WppObject::UserAction(UserAction {
            action: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "UserAction size");
        assert_eq!(WppObject::parse(2368, &bytes), o, "UserAction round trip");
    }
    {
        let o = WppObject::LiveHr(LiveHr {
            hr: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "LiveHr size");
        assert_eq!(WppObject::parse(2369, &bytes), o, "LiveHr round trip");
    }
    {
        let o = WppObject::UnknownSteps(UnknownSteps {
            steps: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "UnknownSteps size");
        assert_eq!(WppObject::parse(2370, &bytes), o, "UnknownSteps round trip");
    }
    {
        let o = WppObject::TimeCounters(TimeCounters {
            rtc_counter_msb: 1,
            rtc_counter_lsb: 2,
            rtc_ms_msb: 3,
            rtc_ms_lsb: 4,
            rtc_seconds: 5,
            utc: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TimeCounters size");
        assert_eq!(WppObject::parse(2371, &bytes), o, "TimeCounters round trip");
    }
    {
        let o = WppObject::SpiFlashCmd(SpiFlashCmd {
            cmd: 1,
            arg1: 2,
            arg2: 3,
            arg3: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SpiFlashCmd size");
        assert_eq!(WppObject::parse(2372, &bytes), o, "SpiFlashCmd round trip");
    }
    {
        let o = WppObject::SpiFlashChunk(SpiFlashChunk {
            chunk: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SpiFlashChunk size");
        assert_eq!(
            WppObject::parse(2373, &bytes),
            o,
            "SpiFlashChunk round trip"
        );
    }
    {
        let o = WppObject::VibratorPattern(VibratorPattern {
            id: 1,
            value: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VibratorPattern size");
        assert_eq!(
            WppObject::parse(2374, &bytes),
            o,
            "VibratorPattern round trip"
        );
    }
    {
        let o = WppObject::HrAs7000Status(HrAs7000Status {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "HrAs7000Status size");
        assert_eq!(
            WppObject::parse(2375, &bytes),
            o,
            "HrAs7000Status round trip"
        );
    }
    {
        let o = WppObject::SleepActivityGet(SleepActivityGet {
            from_utc: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SleepActivityGet size");
        assert_eq!(
            WppObject::parse(2376, &bytes),
            o,
            "SleepActivityGet round trip"
        );
    }
    {
        let o = WppObject::SleepActivity(SleepActivity {
            startdate: 1,
            enddate: 2,
            awake: 3,
            sleep_light: 4,
            sleep_deep: 5,
            sleep_rem: 6,
            wakeupcount: 7,
            durationtosleep: 8,
            completed: 9,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SleepActivity size");
        assert_eq!(
            WppObject::parse(2377, &bytes),
            o,
            "SleepActivity round trip"
        );
    }
    {
        let o = WppObject::EventV1Deprecated(EventV1Deprecated {
            time: 1,
            userid: 2,
            r#type: 3,
            data: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "EventV1Deprecated size");
        assert_eq!(
            WppObject::parse(2378, &bytes),
            o,
            "EventV1Deprecated round trip"
        );
    }
    {
        let o = WppObject::SignalType(SignalType {
            r#type: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SignalType size");
        assert_eq!(WppObject::parse(2379, &bytes), o, "SignalType round trip");
    }
    {
        let o = WppObject::SignalHeader(SignalHeader {
            signaltype: 1,
            version: 2,
            userid: 3,
            signaltime: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SignalHeader size");
        assert_eq!(WppObject::parse(2380, &bytes), o, "SignalHeader round trip");
    }
    {
        let o = WppObject::CborData(CborData {
            buf: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CborData size");
        assert_eq!(WppObject::parse(2381, &bytes), o, "CborData round trip");
    }
    {
        let o = WppObject::AmazonAuth(AmazonAuth {
            auth_code: String::from("wpp1"),
            client_id: String::from("wpp2"),
            redirect_uri: String::from("wpp3"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AmazonAuth size");
        assert_eq!(WppObject::parse(2382, &bytes), o, "AmazonAuth round trip");
    }
    {
        let o = WppObject::No2Cal(No2Cal {
            cmd: 1,
            timestamp: 2,
            reserved1: 3,
            reserved2: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "No2Cal size");
        assert_eq!(WppObject::parse(2383, &bytes), o, "No2Cal round trip");
    }
    {
        let o = WppObject::AmazonCodeChallenge(AmazonCodeChallenge {
            product_id: String::from("wpp1"),
            serial_number: String::from("wpp2"),
            code_challenge: String::from("wpp3"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AmazonCodeChallenge size");
        assert_eq!(
            WppObject::parse(2384, &bytes),
            o,
            "AmazonCodeChallenge round trip"
        );
    }
    {
        let o = WppObject::No2CalMeasure(No2CalMeasure {
            timestamp: 1,
            no2_level: 2,
            rht_t: 3,
            rht_rh: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "No2CalMeasure size");
        assert_eq!(
            WppObject::parse(2385, &bytes),
            o,
            "No2CalMeasure round trip"
        );
    }
    {
        let o = WppObject::SignalData(SignalData {
            buf: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SignalData size");
        assert_eq!(WppObject::parse(2386, &bytes), o, "SignalData round trip");
    }
    {
        let o = WppObject::LapNb(LapNb {
            hour: 1,
            minute: 2,
            tracker: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "LapNb size");
        assert_eq!(WppObject::parse(2387, &bytes), o, "LapNb round trip");
    }
    {
        let o = WppObject::BatteryStateOpt(BatteryStateOpt {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BatteryStateOpt size");
        assert_eq!(
            WppObject::parse(2388, &bytes),
            o,
            "BatteryStateOpt round trip"
        );
    }
    {
        let o = WppObject::ScreenState(ScreenState {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ScreenState size");
        assert_eq!(WppObject::parse(2389, &bytes), o, "ScreenState round trip");
    }
    {
        let o = WppObject::Steps(Steps {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Steps size");
        assert_eq!(WppObject::parse(2390, &bytes), o, "Steps round trip");
    }
    {
        let o = WppObject::Calories(Calories {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Calories size");
        assert_eq!(WppObject::parse(2391, &bytes), o, "Calories round trip");
    }
    {
        let o = WppObject::Distance(Distance {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Distance size");
        assert_eq!(WppObject::parse(2392, &bytes), o, "Distance round trip");
    }
    {
        let o = WppObject::AcqFreq(AcqFreq {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AcqFreq size");
        assert_eq!(WppObject::parse(2393, &bytes), o, "AcqFreq round trip");
    }
    {
        let o = WppObject::InfoType(InfoType {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "InfoType size");
        assert_eq!(WppObject::parse(2394, &bytes), o, "InfoType round trip");
    }
    {
        let o = WppObject::Duration(Duration {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Duration size");
        assert_eq!(WppObject::parse(2395, &bytes), o, "Duration round trip");
    }
    {
        let o = WppObject::GlyphId(GlyphId {
            unicode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "GlyphId size");
        assert_eq!(WppObject::parse(2396, &bytes), o, "GlyphId round trip");
    }
    {
        let o = WppObject::ImageMetadata(ImageMetadata {
            r#type: 1,
            width: 2,
            height: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ImageMetadata size");
        assert_eq!(
            WppObject::parse(2397, &bytes),
            o,
            "ImageMetadata round trip"
        );
    }
    {
        let o = WppObject::ImageData(ImageData {
            data: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ImageData size");
        assert_eq!(WppObject::parse(2398, &bytes), o, "ImageData round trip");
    }
    {
        let o = WppObject::RawDataStreamControl(RawDataStreamControl {
            cmd: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "RawDataStreamControl size");
        assert_eq!(
            WppObject::parse(2400, &bytes),
            o,
            "RawDataStreamControl round trip"
        );
    }
    {
        let o = WppObject::Version(Version {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Version size");
        assert_eq!(WppObject::parse(2401, &bytes), o, "Version round trip");
    }
    {
        let o = WppObject::RawDataReadMode(RawDataReadMode {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "RawDataReadMode size");
        assert_eq!(
            WppObject::parse(2402, &bytes),
            o,
            "RawDataReadMode round trip"
        );
    }
    {
        let o = WppObject::NotificationAppInfo(NotificationAppInfo {
            name: String::from("wpp1"),
            category: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "NotificationAppInfo size");
        assert_eq!(
            WppObject::parse(2403, &bytes),
            o,
            "NotificationAppInfo round trip"
        );
    }
    {
        let o = WppObject::NotificationAppId(NotificationAppId {
            id: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "NotificationAppId size");
        assert_eq!(
            WppObject::parse(2404, &bytes),
            o,
            "NotificationAppId round trip"
        );
    }
    {
        let o = WppObject::NotificationAppDisplayInfo(NotificationAppDisplayInfo {
            mode: 1,
            display: 2,
            fields_order: vec![3, 4],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "NotificationAppDisplayInfo size"
        );
        assert_eq!(
            WppObject::parse(2405, &bytes),
            o,
            "NotificationAppDisplayInfo round trip"
        );
    }
    {
        let o = WppObject::NotificationAppHashIdVersion(NotificationAppHashIdVersion {
            version: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "NotificationAppHashIdVersion size"
        );
        assert_eq!(
            WppObject::parse(2406, &bytes),
            o,
            "NotificationAppHashIdVersion round trip"
        );
    }
    {
        let o = WppObject::NotificationAppHashIdCrc32(NotificationAppHashIdCrc32 {
            hash_id: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "NotificationAppHashIdCrc32 size"
        );
        assert_eq!(
            WppObject::parse(2407, &bytes),
            o,
            "NotificationAppHashIdCrc32 round trip"
        );
    }
    {
        let o = WppObject::CacheType(CacheType {
            r#type: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CacheType size");
        assert_eq!(WppObject::parse(2408, &bytes), o, "CacheType round trip");
    }
    {
        let o = WppObject::ActivitySubcategory(ActivitySubcategory {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ActivitySubcategory size");
        assert_eq!(
            WppObject::parse(2409, &bytes),
            o,
            "ActivitySubcategory round trip"
        );
    }
    {
        let o = WppObject::Timestamp(Timestamp {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Timestamp size");
        assert_eq!(WppObject::parse(2410, &bytes), o, "Timestamp round trip");
    }
    {
        let o = WppObject::Pace(Pace {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Pace size");
        assert_eq!(WppObject::parse(2411, &bytes), o, "Pace round trip");
    }
    {
        let o = WppObject::FaceMode(FaceMode {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FaceMode size");
        assert_eq!(WppObject::parse(2412, &bytes), o, "FaceMode round trip");
    }
    {
        let o = WppObject::Speed(Speed {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Speed size");
        assert_eq!(WppObject::parse(2413, &bytes), o, "Speed round trip");
    }
    {
        let o = WppObject::FwInfo(FwInfo {
            version: 1,
            size: 2,
            crc: 3,
            sha1: vec![4, 5],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FwInfo size");
        assert_eq!(WppObject::parse(2414, &bytes), o, "FwInfo round trip");
    }
    {
        let o = WppObject::FwChunkRequest(FwChunkRequest {
            version: 1,
            offset: 2,
            size: 3,
            percent: 4,
            packet_max_size: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FwChunkRequest size");
        assert_eq!(
            WppObject::parse(2415, &bytes),
            o,
            "FwChunkRequest round trip"
        );
    }
    {
        let o = WppObject::FwChunk(FwChunk {
            offset: 1,
            packet: vec![2, 3],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FwChunk size");
        assert_eq!(WppObject::parse(2416, &bytes), o, "FwChunk round trip");
    }
    {
        let o = WppObject::FwChunkCrc(FwChunkCrc {
            offset: 1,
            size: 2,
            crc: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FwChunkCrc size");
        assert_eq!(WppObject::parse(2417, &bytes), o, "FwChunkCrc round trip");
    }
    {
        let o = WppObject::StartTime(StartTime {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StartTime size");
        assert_eq!(WppObject::parse(2418, &bytes), o, "StartTime round trip");
    }
    {
        let o = WppObject::EndTime(EndTime {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "EndTime size");
        assert_eq!(WppObject::parse(2419, &bytes), o, "EndTime round trip");
    }
    {
        let o = WppObject::Status(Status {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Status size");
        assert_eq!(WppObject::parse(2420, &bytes), o, "Status round trip");
    }
    {
        let o = WppObject::TestScreen(TestScreen {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TestScreen size");
        assert_eq!(WppObject::parse(2421, &bytes), o, "TestScreen round trip");
    }
    {
        let o = WppObject::ChallengeRequest(ChallengeRequest {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ChallengeRequest size");
        assert_eq!(
            WppObject::parse(2422, &bytes),
            o,
            "ChallengeRequest round trip"
        );
    }
    {
        let o = WppObject::BleShellChallenge(BleShellChallenge {
            challenge: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BleShellChallenge size");
        assert_eq!(
            WppObject::parse(2423, &bytes),
            o,
            "BleShellChallenge round trip"
        );
    }
    {
        let o = WppObject::DebugDumpAnchor(DebugDumpAnchor {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DebugDumpAnchor size");
        assert_eq!(
            WppObject::parse(2424, &bytes),
            o,
            "DebugDumpAnchor round trip"
        );
    }
    {
        let o = WppObject::FactoryMode(FactoryMode {
            mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FactoryMode size");
        assert_eq!(WppObject::parse(2425, &bytes), o, "FactoryMode round trip");
    }
    {
        let o = WppObject::GlanceStatus(GlanceStatus {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "GlanceStatus size");
        assert_eq!(WppObject::parse(2426, &bytes), o, "GlanceStatus round trip");
    }
    {
        let o = WppObject::MeasureCategory(MeasureCategory {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MeasureCategory size");
        assert_eq!(
            WppObject::parse(2427, &bytes),
            o,
            "MeasureCategory round trip"
        );
    }
    {
        let o = WppObject::MeasureLiveMeta(MeasureLiveMeta {
            time_window: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MeasureLiveMeta size");
        assert_eq!(
            WppObject::parse(2428, &bytes),
            o,
            "MeasureLiveMeta round trip"
        );
    }
    {
        let o = WppObject::MeasureLiveEcg(MeasureLiveEcg {
            samples: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MeasureLiveEcg size");
        assert_eq!(
            WppObject::parse(2429, &bytes),
            o,
            "MeasureLiveEcg round trip"
        );
    }
    {
        let o = WppObject::MeasureStopReason(MeasureStopReason {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MeasureStopReason size");
        assert_eq!(
            WppObject::parse(2430, &bytes),
            o,
            "MeasureStopReason round trip"
        );
    }
    {
        let o = WppObject::PressureTemperature(PressureTemperature {
            pressure: 1,
            temperature: 2,
            calib_coefs: vec![3, 4],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "PressureTemperature size");
        assert_eq!(
            WppObject::parse(2431, &bytes),
            o,
            "PressureTemperature round trip"
        );
    }
    {
        let o = WppObject::AltimeterCompensation(AltimeterCompensation {
            val: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AltimeterCompensation size");
        assert_eq!(
            WppObject::parse(2432, &bytes),
            o,
            "AltimeterCompensation round trip"
        );
    }
    {
        let o = WppObject::Percentile(Percentile {
            val: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Percentile size");
        assert_eq!(WppObject::parse(2433, &bytes), o, "Percentile round trip");
    }
    {
        let o = WppObject::BatteryStatusSamples(BatteryStatusSamples {
            nb_samples: 1,
            values: vec![2, 3],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BatteryStatusSamples size");
        assert_eq!(
            WppObject::parse(2434, &bytes),
            o,
            "BatteryStatusSamples round trip"
        );
    }
    {
        let o = WppObject::MeasureProcessStep(MeasureProcessStep {
            step: 1,
            step_param: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MeasureProcessStep size");
        assert_eq!(
            WppObject::parse(2435, &bytes),
            o,
            "MeasureProcessStep round trip"
        );
    }
    {
        let o = WppObject::ActivityLap(ActivityLap {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ActivityLap size");
        assert_eq!(WppObject::parse(2436, &bytes), o, "ActivityLap round trip");
    }
    {
        let o = WppObject::Udi(Udi {
            r#type: 1,
            value: String::from("wpp2"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Udi size");
        assert_eq!(WppObject::parse(2437, &bytes), o, "Udi round trip");
    }
    {
        let o = WppObject::ActivityPause(ActivityPause {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ActivityPause size");
        assert_eq!(
            WppObject::parse(2438, &bytes),
            o,
            "ActivityPause round trip"
        );
    }
    {
        let o = WppObject::PauseState(PauseState {
            value: 1,
            start_time: 2,
            sum: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "PauseState size");
        assert_eq!(WppObject::parse(2439, &bytes), o, "PauseState round trip");
    }
    {
        let o = WppObject::WorkoutLapState(WorkoutLapState {
            value: 1,
            time: 2,
            diff: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutLapState size");
        assert_eq!(
            WppObject::parse(2440, &bytes),
            o,
            "WorkoutLapState round trip"
        );
    }
    {
        let o = WppObject::WorkoutTotalDiff(WorkoutTotalDiff {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutTotalDiff size");
        assert_eq!(
            WppObject::parse(2441, &bytes),
            o,
            "WorkoutTotalDiff round trip"
        );
    }
    {
        let o = WppObject::RebootReason(RebootReason {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "RebootReason size");
        assert_eq!(WppObject::parse(2442, &bytes), o, "RebootReason round trip");
    }
    {
        let o = WppObject::RebootOptional(RebootOptional {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "RebootOptional size");
        assert_eq!(
            WppObject::parse(2443, &bytes),
            o,
            "RebootOptional round trip"
        );
    }
    {
        let o = WppObject::Max8614xFactoryStats(Max8614xFactoryStats {
            ac_red: 1,
            ac_red_pa: 2,
            dc_red: 3,
            dc_red_pa: 4,
            ac_ir: 5,
            ac_ir_pa: 6,
            dc_ir: 7,
            dc_ir_pa: 8,
            ac_green: 9,
            ac_green_pa: 10,
            dc_green: 11,
            dc_green_pa: 12,
            r: 13,
            led_value_red: 14,
            led_range_red: 15,
            adc_range_red: 16,
            led_value_ir: 17,
            led_range_ir: 18,
            adc_range_ir: 19,
            led_value_green: 20,
            led_range_green: 21,
            adc_range_green: 22,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Max8614xFactoryStats size");
        assert_eq!(
            WppObject::parse(2444, &bytes),
            o,
            "Max8614xFactoryStats round trip"
        );
    }
    {
        let o = WppObject::DigitalCrownMotionDelta(DigitalCrownMotionDelta {
            x: 1,
            y: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DigitalCrownMotionDelta size");
        assert_eq!(
            WppObject::parse(2445, &bytes),
            o,
            "DigitalCrownMotionDelta round trip"
        );
    }
    {
        let o = WppObject::DigitalCrownResolutionParams(DigitalCrownResolutionParams {
            x: 1,
            y: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "DigitalCrownResolutionParams size"
        );
        assert_eq!(
            WppObject::parse(2446, &bytes),
            o,
            "DigitalCrownResolutionParams round trip"
        );
    }
    {
        let o = WppObject::Max8614xFactoryStatsParams(Max8614xFactoryStatsParams {
            mode: 1,
            led1: 2,
            led2: 3,
            led3: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "Max8614xFactoryStatsParams size"
        );
        assert_eq!(
            WppObject::parse(2447, &bytes),
            o,
            "Max8614xFactoryStatsParams round trip"
        );
    }
    {
        let o = WppObject::MtuWpp(MtuWpp {
            max_recv_size: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MtuWpp size");
        assert_eq!(WppObject::parse(2448, &bytes), o, "MtuWpp round trip");
    }
    {
        let o = WppObject::MtuTls(MtuTls {
            mtu: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MtuTls size");
        assert_eq!(WppObject::parse(2449, &bytes), o, "MtuTls round trip");
    }
    {
        let o = WppObject::MtuAttBle(MtuAttBle {
            mtu: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MtuAttBle size");
        assert_eq!(WppObject::parse(2450, &bytes), o, "MtuAttBle round trip");
    }
    {
        let o = WppObject::StoredMeasureDataExtendPos(StoredMeasureDataExtendPos {
            position: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "StoredMeasureDataExtendPos size"
        );
        assert_eq!(
            WppObject::parse(2451, &bytes),
            o,
            "StoredMeasureDataExtendPos round trip"
        );
    }
    {
        let o = WppObject::Heartrate(Heartrate {
            heartrate: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Heartrate size");
        assert_eq!(WppObject::parse(2452, &bytes), o, "Heartrate round trip");
    }
    {
        let o = WppObject::VasistasSpo2(VasistasSpo2 {
            spo2: 1,
            quality: 2,
            error: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasSpo2 size");
        assert_eq!(WppObject::parse(2453, &bytes), o, "VasistasSpo2 round trip");
    }
    {
        let o = WppObject::Key(Key {
            val: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Key size");
        assert_eq!(WppObject::parse(2455, &bytes), o, "Key round trip");
    }
    {
        let o = WppObject::CovidEbidEcc(CovidEbidEcc {
            epoch: 1,
            ebid: 2,
            ecc: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CovidEbidEcc size");
        assert_eq!(WppObject::parse(2456, &bytes), o, "CovidEbidEcc round trip");
    }
    {
        let o = WppObject::CovidStatusAtRisk(CovidStatusAtRisk {
            last_exposure_timeframe: 1,
            at_risk: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CovidStatusAtRisk size");
        assert_eq!(
            WppObject::parse(2457, &bytes),
            o,
            "CovidStatusAtRisk round trip"
        );
    }
    {
        let o = WppObject::CovidHelloReport(CovidHelloReport {
            ebid: 1,
            time_collected_on_device: 2,
            time_from_hello_message: 3,
            hmac: vec![4, 5],
            rssi: 5,
            ecc: 6,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CovidHelloReport size");
        assert_eq!(
            WppObject::parse(2458, &bytes),
            o,
            "CovidHelloReport round trip"
        );
    }
    {
        let o = WppObject::Hmac(Hmac {
            val: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Hmac size");
        assert_eq!(WppObject::parse(2459, &bytes), o, "Hmac round trip");
    }
    {
        let o = WppObject::FeatureTagsDeprecated(FeatureTagsDeprecated {
            id: 1,
            start_time: 2,
            end_time: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FeatureTagsDeprecated size");
        assert_eq!(
            WppObject::parse(2460, &bytes),
            o,
            "FeatureTagsDeprecated round trip"
        );
    }
    {
        let o = WppObject::VasistasFlags(VasistasFlags {
            enabled_flags: 1,
            supported_flags: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasFlags size");
        assert_eq!(
            WppObject::parse(2461, &bytes),
            o,
            "VasistasFlags round trip"
        );
    }
    {
        let o = WppObject::MeasureLiveAppStatus(MeasureLiveAppStatus {
            app_live_screen_displayed: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MeasureLiveAppStatus size");
        assert_eq!(
            WppObject::parse(2463, &bytes),
            o,
            "MeasureLiveAppStatus round trip"
        );
    }
    {
        let o = WppObject::VasistasAhi(VasistasAhi {
            ahi: 1,
            bd_proba: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasAhi size");
        assert_eq!(WppObject::parse(2464, &bytes), o, "VasistasAhi round trip");
    }
    {
        let o = WppObject::ShortcutAction(ShortcutAction {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ShortcutAction size");
        assert_eq!(
            WppObject::parse(2465, &bytes),
            o,
            "ShortcutAction round trip"
        );
    }
    {
        let o = WppObject::BootstrapRedirect(BootstrapRedirect {
            ws_error_code: 1,
            https_subdomain: String::from("wpp2"),
            wpps_subdomain: String::from("wpp3"),
            domain: String::from("wpp4"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BootstrapRedirect size");
        assert_eq!(
            WppObject::parse(2467, &bytes),
            o,
            "BootstrapRedirect round trip"
        );
    }
    {
        let o = WppObject::CertDer(CertDer {
            data: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CertDer size");
        assert_eq!(WppObject::parse(2468, &bytes), o, "CertDer round trip");
    }
    {
        let o = WppObject::Threshold(Threshold {
            id: 1,
            enabled: 2,
            exponent: 3,
            value: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Threshold size");
        assert_eq!(WppObject::parse(2469, &bytes), o, "Threshold round trip");
    }
    {
        let o = WppObject::Shortid(Shortid {
            id: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Shortid size");
        assert_eq!(WppObject::parse(2470, &bytes), o, "Shortid round trip");
    }
    {
        let o = WppObject::NotificationsDisplayState(NotificationsDisplayState {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "NotificationsDisplayState size");
        assert_eq!(
            WppObject::parse(2471, &bytes),
            o,
            "NotificationsDisplayState round trip"
        );
    }
    {
        let o = WppObject::LocalNotification(LocalNotification {
            id: 1,
            status: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "LocalNotification size");
        assert_eq!(
            WppObject::parse(2472, &bytes),
            o,
            "LocalNotification round trip"
        );
    }
    {
        let o = WppObject::EventV2(EventV2 {
            r#type: 1,
            epoch: 2,
            userid: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "EventV2 size");
        assert_eq!(WppObject::parse(2473, &bytes), o, "EventV2 round trip");
    }
    {
        let o = WppObject::Boolean(Boolean {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Boolean size");
        assert_eq!(WppObject::parse(2474, &bytes), o, "Boolean round trip");
    }
    {
        let o = WppObject::EcgWaveItvl(EcgWaveItvl {
            qt: 1,
            qtc: 2,
            pq: 3,
            qrs: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "EcgWaveItvl size");
        assert_eq!(WppObject::parse(2475, &bytes), o, "EcgWaveItvl round trip");
    }
    {
        let o = WppObject::BreatheParam(BreatheParam {
            id: 1,
            value: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BreatheParam size");
        assert_eq!(WppObject::parse(2476, &bytes), o, "BreatheParam round trip");
    }
    {
        let o = WppObject::InactivityCfg(InactivityCfg {
            from_hour: 1,
            from_minute: 2,
            to_hour: 3,
            to_minute: 4,
            wday: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "InactivityCfg size");
        assert_eq!(
            WppObject::parse(2477, &bytes),
            o,
            "InactivityCfg round trip"
        );
    }
    {
        let o = WppObject::ConsumableStatus(ConsumableStatus {
            state: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "ConsumableStatus size");
        assert_eq!(
            WppObject::parse(2478, &bytes),
            o,
            "ConsumableStatus round trip"
        );
    }
    {
        let o = WppObject::StripCount(StripCount {
            strip_type: 1,
            count: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StripCount size");
        assert_eq!(WppObject::parse(2479, &bytes), o, "StripCount round trip");
    }
    {
        let o = WppObject::Max8614xChannelStats(Max8614xChannelStats {
            channel_id: 1,
            ac_adc: 2,
            ac_current_pa: 3,
            dc_adc: 4,
            dc_current_pa: 5,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Max8614xChannelStats size");
        assert_eq!(
            WppObject::parse(2480, &bytes),
            o,
            "Max8614xChannelStats round trip"
        );
    }
    {
        let o = WppObject::Max8614xChannelConfig(Max8614xChannelConfig {
            channel_id: 1,
            led_value: 2,
            led_range: 3,
            adc_range: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Max8614xChannelConfig size");
        assert_eq!(
            WppObject::parse(2481, &bytes),
            o,
            "Max8614xChannelConfig round trip"
        );
    }
    {
        let o = WppObject::Max8614xR(Max8614xR {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Max8614xR size");
        assert_eq!(WppObject::parse(2482, &bytes), o, "Max8614xR round trip");
    }
    {
        let o = WppObject::MacBytes(MacBytes {
            mac: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MacBytes size");
        assert_eq!(WppObject::parse(2483, &bytes), o, "MacBytes round trip");
    }
    {
        let o = WppObject::IsCartridgeInserted(IsCartridgeInserted {
            is_cartridge_inserted: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "IsCartridgeInserted size");
        assert_eq!(
            WppObject::parse(2484, &bytes),
            o,
            "IsCartridgeInserted round trip"
        );
    }
    {
        let o = WppObject::IsHoodOpened(IsHoodOpened {
            is_hood_opened: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "IsHoodOpened size");
        assert_eq!(WppObject::parse(2485, &bytes), o, "IsHoodOpened round trip");
    }
    {
        let o = WppObject::Max8617xChannelStats(Max8617xChannelStats {
            channel_id: 1,
            channel_name: String::from("wpp2"),
            std_adc: 3,
            std_current_pa: 4,
            avg_adc: 5,
            avg_current_pa: 6,
            led_value: 7,
            adc_range: 8,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Max8617xChannelStats size");
        assert_eq!(
            WppObject::parse(2486, &bytes),
            o,
            "Max8617xChannelStats round trip"
        );
    }
    {
        let o = WppObject::Mcp3422MeasureResult(Mcp3422MeasureResult {
            value_uv: 1,
            value_adc: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Mcp3422MeasureResult size");
        assert_eq!(
            WppObject::parse(2487, &bytes),
            o,
            "Mcp3422MeasureResult round trip"
        );
    }
    {
        let o = WppObject::As6221MeasureResult(As6221MeasureResult {
            temp_q7: 1,
            integer: 2,
            fraction: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "As6221MeasureResult size");
        assert_eq!(
            WppObject::parse(2488, &bytes),
            o,
            "As6221MeasureResult round trip"
        );
    }
    {
        let o = WppObject::AlgoParam(AlgoParam {
            id: 1,
            value: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AlgoParam size");
        assert_eq!(WppObject::parse(2489, &bytes), o, "AlgoParam round trip");
    }
    {
        let o = WppObject::Stairs(Stairs {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Stairs size");
        assert_eq!(WppObject::parse(2490, &bytes), o, "Stairs round trip");
    }
    {
        let o = WppObject::MoveHands(MoveHands {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "MoveHands size");
        assert_eq!(WppObject::parse(2491, &bytes), o, "MoveHands round trip");
    }
    {
        let o = WppObject::CartridgeExpiryDate(CartridgeExpiryDate {
            effective_expiry_date: 1,
            printed_expiry_date: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CartridgeExpiryDate size");
        assert_eq!(
            WppObject::parse(2492, &bytes),
            o,
            "CartridgeExpiryDate round trip"
        );
    }
    {
        let o = WppObject::WorkoutMaxNumber(WorkoutMaxNumber {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "WorkoutMaxNumber size");
        assert_eq!(
            WppObject::parse(2493, &bytes),
            o,
            "WorkoutMaxNumber round trip"
        );
    }
    {
        let o = WppObject::Tmp117MeasureResult(Tmp117MeasureResult {
            temp_q7: 1,
            integer: 2,
            fraction: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Tmp117MeasureResult size");
        assert_eq!(
            WppObject::parse(2494, &bytes),
            o,
            "Tmp117MeasureResult round trip"
        );
    }
    {
        let o = WppObject::Sn19020x6MeasureResult(Sn19020x6MeasureResult {
            value_uv: 1,
            value_adc: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Sn19020x6MeasureResult size");
        assert_eq!(
            WppObject::parse(2495, &bytes),
            o,
            "Sn19020x6MeasureResult round trip"
        );
    }
    {
        let o = WppObject::SkinTemperatureMeasureResult(SkinTemperatureMeasureResult {
            temp_q7: 1,
            integer: 2,
            fraction: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "SkinTemperatureMeasureResult size"
        );
        assert_eq!(
            WppObject::parse(2496, &bytes),
            o,
            "SkinTemperatureMeasureResult round trip"
        );
    }
    {
        let o = WppObject::FluxSensorMeasureResult(FluxSensorMeasureResult {
            value_uv: 1,
            value_adc: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FluxSensorMeasureResult size");
        assert_eq!(
            WppObject::parse(2497, &bytes),
            o,
            "FluxSensorMeasureResult round trip"
        );
    }
    {
        let o = WppObject::DeviceChallengeRequest(DeviceChallengeRequest {
            platform_random: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DeviceChallengeRequest size");
        assert_eq!(
            WppObject::parse(2498, &bytes),
            o,
            "DeviceChallengeRequest round trip"
        );
    }
    {
        let o = WppObject::DeviceChallengeReply(DeviceChallengeReply {
            platform_random: vec![1, 2],
            device_random: vec![2, 3],
            signature_algo_id: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DeviceChallengeReply size");
        assert_eq!(
            WppObject::parse(2499, &bytes),
            o,
            "DeviceChallengeReply round trip"
        );
    }
    {
        let o = WppObject::DeviceChallengeSignature(DeviceChallengeSignature {
            data: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DeviceChallengeSignature size");
        assert_eq!(
            WppObject::parse(2500, &bytes),
            o,
            "DeviceChallengeSignature round trip"
        );
    }
    {
        let o = WppObject::FactoryTestDsc(FactoryTestDsc {
            r#type: 1,
            status: 2,
            arg: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FactoryTestDsc size");
        assert_eq!(
            WppObject::parse(2501, &bytes),
            o,
            "FactoryTestDsc round trip"
        );
    }
    {
        let o = WppObject::VasistasCbt(VasistasCbt {
            algo: 1,
            attrib: 2,
            temperature: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasCbt size");
        assert_eq!(WppObject::parse(2502, &bytes), o, "VasistasCbt round trip");
    }
    {
        let o = WppObject::CleansingModeStart(CleansingModeStart {
            mode: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CleansingModeStart size");
        assert_eq!(
            WppObject::parse(2503, &bytes),
            o,
            "CleansingModeStart round trip"
        );
    }
    {
        let o = WppObject::CleansingModeStatus(CleansingModeStatus {
            status: 1,
            timeout_wait_mixture_sec: 2,
            timeout_cleansing_ongoing_sec: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CleansingModeStatus size");
        assert_eq!(
            WppObject::parse(2504, &bytes),
            o,
            "CleansingModeStatus round trip"
        );
    }
    {
        let o = WppObject::Max86173ChannelStats(Max86173ChannelStats {
            tag: 1,
            channel_name: String::from("wpp2"),
            avg_paperma: 3,
            avg_adc: 4,
            std_adc: 5,
            led_value: 6,
            adc_range: 7,
            led_range: 8,
            agc_changes: 9,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Max86173ChannelStats size");
        assert_eq!(
            WppObject::parse(2505, &bytes),
            o,
            "Max86173ChannelStats round trip"
        );
    }
    {
        let o = WppObject::GreentegSensitivityBin(GreentegSensitivityBin {
            reel_char: String::from("wpp1"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "GreentegSensitivityBin size");
        assert_eq!(
            WppObject::parse(2506, &bytes),
            o,
            "GreentegSensitivityBin round trip"
        );
    }
    {
        let o = WppObject::DigitalCrownScaleFactor(DigitalCrownScaleFactor {
            numerator: 1,
            denominator: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DigitalCrownScaleFactor size");
        assert_eq!(
            WppObject::parse(2507, &bytes),
            o,
            "DigitalCrownScaleFactor round trip"
        );
    }
    {
        let o = WppObject::SymptomType(SymptomType {
            id: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SymptomType size");
        assert_eq!(WppObject::parse(2508, &bytes), o, "SymptomType round trip");
    }
    {
        let o = WppObject::SymptomListItem(SymptomListItem {
            id: 1,
            activated: 2,
            timestamp: 3,
            label: String::from("wpp4"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SymptomListItem size");
        assert_eq!(
            WppObject::parse(2509, &bytes),
            o,
            "SymptomListItem round trip"
        );
    }
    {
        let o = WppObject::CycleTrackingCycle(CycleTrackingCycle {
            start: 1,
            confirmed_start: 2,
            confirmed_end: 3,
            predicted_period_start: 4,
            predicted_period_end: 5,
            predicted_ovulation_start: 6,
            predicted_ovulation_end: 7,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "CycleTrackingCycle size");
        assert_eq!(
            WppObject::parse(2510, &bytes),
            o,
            "CycleTrackingCycle round trip"
        );
    }
    {
        let o = WppObject::VibratorSetSimple(VibratorSetSimple {
            duration_on_ms: 1,
            duration_off_ms: 2,
            nb_loop: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VibratorSetSimple size");
        assert_eq!(
            WppObject::parse(2511, &bytes),
            o,
            "VibratorSetSimple round trip"
        );
    }
    {
        let o = WppObject::CycleTrackingMenstruationStart(CycleTrackingMenstruationStart {
            timestamp: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(
            bytes.len(),
            o.data_size(),
            "CycleTrackingMenstruationStart size"
        );
        assert_eq!(
            WppObject::parse(2512, &bytes),
            o,
            "CycleTrackingMenstruationStart round trip"
        );
    }
    {
        let o = WppObject::DeviceStatus(DeviceStatus {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "DeviceStatus size");
        assert_eq!(WppObject::parse(2513, &bytes), o, "DeviceStatus round trip");
    }
    {
        let o = WppObject::TrackerHistory(TrackerHistory {
            day0: 1,
            day1: 2,
            day2: 3,
            day3: 4,
            day4: 5,
            day5: 6,
            day6: 7,
            history_type: 8,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "TrackerHistory size");
        assert_eq!(
            WppObject::parse(2514, &bytes),
            o,
            "TrackerHistory round trip"
        );
    }
    {
        let o = WppObject::SecureFwupdateRand(SecureFwupdateRand {
            device_random: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SecureFwupdateRand size");
        assert_eq!(
            WppObject::parse(2515, &bytes),
            o,
            "SecureFwupdateRand round trip"
        );
    }
    {
        let o = WppObject::SecureFwupdateSign(SecureFwupdateSign {
            sha512: vec![1, 2],
            sign_ed25519: vec![2, 3],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SecureFwupdateSign size");
        assert_eq!(
            WppObject::parse(2516, &bytes),
            o,
            "SecureFwupdateSign round trip"
        );
    }
    {
        let o = WppObject::FeatureTags(FeatureTags {
            start_time: 1,
            end_time: 2,
            id: 3,
            status: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "FeatureTags size");
        assert_eq!(WppObject::parse(2517, &bytes), o, "FeatureTags round trip");
    }
    {
        let o = WppObject::GreentegIntegrationFactor(GreentegIntegrationFactor {
            value: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "GreentegIntegrationFactor size");
        assert_eq!(
            WppObject::parse(2518, &bytes),
            o,
            "GreentegIntegrationFactor round trip"
        );
    }
    {
        let o = WppObject::BleIrk(BleIrk {
            bytes: vec![1, 2],
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "BleIrk size");
        assert_eq!(WppObject::parse(2519, &bytes), o, "BleIrk round trip");
    }
    {
        let o = WppObject::VasistasHrv(VasistasHrv {
            hr: 1,
            sdnn: 2,
            rmssd: 3,
            quality: 4,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasHrv size");
        assert_eq!(WppObject::parse(2520, &bytes), o, "VasistasHrv round trip");
    }
    {
        let o = WppObject::VasistasRr(VasistasRr {
            rr: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasRr size");
        assert_eq!(WppObject::parse(2521, &bytes), o, "VasistasRr round trip");
    }
    {
        let o = WppObject::VasistasActivityType(VasistasActivityType {
            activity_subcategory: 1,
            level: 2,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasActivityType size");
        assert_eq!(
            WppObject::parse(2522, &bytes),
            o,
            "VasistasActivityType round trip"
        );
    }
    {
        let o = WppObject::VasistasDbt(VasistasDbt {
            hr_max_avg: 1,
            hr_min_avg: 2,
            in_period_ds: 3,
            ex_period_ds: 4,
            in_period_target_s: 5,
            ex_period_target_s: 6,
            duration_target: 7,
            is_from_ecg: 8,
            hr_quality: 9,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "VasistasDbt size");
        assert_eq!(WppObject::parse(2523, &bytes), o, "VasistasDbt round trip");
    }
    {
        let o = WppObject::GreentegParameter(GreentegParameter {
            r#type: 1,
            value: 2,
            factory: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "GreentegParameter size");
        assert_eq!(
            WppObject::parse(2524, &bytes),
            o,
            "GreentegParameter round trip"
        );
    }
    {
        let o = WppObject::AccelerometerValues(AccelerometerValues {
            x: 1,
            y: 2,
            z: 3,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "AccelerometerValues size");
        assert_eq!(
            WppObject::parse(2525, &bytes),
            o,
            "AccelerometerValues round trip"
        );
    }
    {
        let o = WppObject::PushButtonTestParameter(PushButtonTestParameter {
            timeout: 1,
            gpio_str: String::from("wpp2"),
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "PushButtonTestParameter size");
        assert_eq!(
            WppObject::parse(2526, &bytes),
            o,
            "PushButtonTestParameter round trip"
        );
    }
    {
        let o = WppObject::PushButtonTestStatus(PushButtonTestStatus {
            status: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "PushButtonTestStatus size");
        assert_eq!(
            WppObject::parse(2527, &bytes),
            o,
            "PushButtonTestStatus round trip"
        );
    }
    {
        let o = WppObject::Max8619xChannelStats(Max8619xChannelStats {
            mode: String::from("wpp1"),
            channel_id: 2,
            channel_name: String::from("wpp3"),
            avg_adc: 4,
            std_adc: 5,
            led_current_ma: 6,
            adc_range: 7,
            led_range: 8,
            avg_paperma: 9,
            channel_tint: 10,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "Max8619xChannelStats size");
        assert_eq!(
            WppObject::parse(2528, &bytes),
            o,
            "Max8619xChannelStats round trip"
        );
    }
    {
        let o = WppObject::StatusChanged(StatusChanged {
            prog: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "StatusChanged size");
        assert_eq!(
            WppObject::parse(32768, &bytes),
            o,
            "StatusChanged round trip"
        );
    }
    {
        let o = WppObject::SettingsChanged(SettingsChanged {
            prog: 1,
            ..Default::default()
        });
        let mut w = crate::codec::Writer::new();
        o.write_data(&mut w);
        let bytes = w.finish();
        assert_eq!(bytes.len(), o.data_size(), "SettingsChanged size");
        assert_eq!(
            WppObject::parse(32769, &bytes),
            o,
            "SettingsChanged round trip"
        );
    }
}
