# Each newlib archive abi/refbuild.sh builds, by its code bytes
# (text+data+bss of every member). The file size is not a measurement:
# every member carries the source path in its DWARF, so the same archive
# built from a differently named tree is a different number of bytes with
# identical text. abi/refbuild.sh --check reports the tree against this.
newlib-nano-big      libc.a   137220
newlib-nano-big      libm.a   83648
newlib-nano-small    libc.a   140301
newlib-nano-small    libm.a   83648
newlib-nano-ll       libc.a   138129
newlib-nano-ll       libm.a   83648
newlib-nano-ll-st    libc.a   135263
newlib-nano-ll-st    libm.a   83648
