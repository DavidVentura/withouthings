# Firmware patch list for the ABI overlay. One patch per line:
#   <address> <original bytes> <new bytes>   # why
# Addresses are flash offsets, which equal virtual addresses for the internal
# flash image. patch.py refuses to apply a patch whose original bytes differ.
# This list is the firmware's; machine.resc's monitor writes are the
# simulator's own fidelity patches and must not be mixed in here.

# wpp_cmd_table[23].handler, the WPP_CMD_BATTERY_STATUS (1284) entry:
# 0x6ca91 -> the overlay entry at 0xf4000, which logs and then tail-calls it.
0x27aac 91 ca 06 00  01 40 0f 00
